# GoTo2.5D 工程架构说明

> 2.5D 俯视角游戏工程（Unity **2022.3.62f3c1**）· 版本控制：Git
> 本文件由源码 + 各子系统 README 整理生成，随代码演进请同步更新。

---

## 1. 工程总览

GoTo2.5D 是一个以 **可视化节点图（xNode）驱动玩法逻辑** 的 Unity 游戏工程。
核心设计思想：**玩法逻辑尽量不进 C# 代码，而是在节点图资产里拖出来**；
C# 侧只负责提供【地基】——变量存储、事件总线、存档、执行器、场景行为。

```
事件系统(Event)        ← 泛型事件总线，支撑通讯 / 持久变量节点
变量系统(Variable)     ← 数据地基：节点图/房间/全局变量、物品状态共用
    └─ 存档系统(Save)  → 两层存档（staging/archive），持久化变量与物品
节点图系统(xNode)      ← 可视化玩法逻辑（本图变量/跨图通讯/持久变量/存档点）
```

### 顶层目录结构

```
To2.5D/                        # Git 仓库根
├── README.md                  # 项目说明（系统索引）
├── 架构说明.md                 # 本文档
└── GoTo2.5D/                  # Unity 工程根
    ├── Assets/
    │   ├── Scenes/            # 002.unity、开始.unity
    │   ├── Scripts/
    │   │   ├── Base/          # 核心地基（6 大系统）
    │   │   │   ├── Event/     # 事件总线
    │   │   │   ├── Variable/  # 变量系统
    │   │   │   ├── Save/      # 存档系统
    │   │   │   ├── Music/     # 音乐系统
    │   │   │   ├── UI/        # UI 系统
    │   │   │   └── xNode/     # 节点图系统
    │   │   └── Scene_开始/    # 开始场景专用脚本（UI / Graph）
    │   ├── Plugins/           # 第三方插件
    │   │   ├── DOTween        # 补间动画
    │   │   ├── Koreographer   # 音乐节奏系统
    │   │   ├── TMPEffects     # 文本特效
    │   │   ├── Editor/        # 编辑器扩展
    │   │   └── ...            # 含 xNode 本体
    │   ├── Resources/         # 运行时加载资源
    │   │   ├── PersistentVariables/Room/   # 房间变量资产（按场景名）
    │   │   ├── Music/ Sound/ UI/ Prefabs/ Other/...
    │   └── TextMesh Pro/      # TMP 资源
    ├── ProjectSettings/       # 工程设置
    └── Packages/              # 包清单（URP 等）
```

---

## 2. 事件系统（Event）

> 位置：`Assets/Scripts/Base/Event` · 命名空间 `ZGameFramework.Core`

轻量级**泛型事件总线**，带对象池回收，用于解耦各系统（尤其节点图通讯）。

| 类 | 说明 |
|---|---|
| `EventBus` | 静态事件总线（发布 / 订阅 / 退订） |
| `GameEvent` | 事件基类，实现 `IPoolable` |
| `ParameterizedEvent<T>` | 泛型参数事件：`Trigger` / `Subscribe` / `Unsubscribe` |
| `SignalEvent<T>` | 无参信号事件 |
| `ClassPool<T>` / `ListPool<T>` | 对象池（`IPoolable` 配合） |

**关键语义**：

- `Trigger` **同步发布**，订阅者立刻收到，回调内可安全读数据；
- 事件对象从池复用，实现 `OnRecycled()` 清理字段防止脏数据。

```csharp
public class MyEvent : ParameterizedEvent<MyEvent>
{
    public int value;
    public override void OnRecycled() { value = 0; }
}
MyEvent.Subscribe(e => Debug.Log(e.value));
MyEvent.Trigger(e => e.value = 42);   // 同步派发
```

---

## 3. 变量系统（Variable）

> 位置：`Assets/Scripts/Base/Variable`

**统一变量存储体系**：节点图变量、房间变量、全局变量、物品状态共用同一套机制。

### 3.1 核心类

| 类 | 说明 |
|---|---|
| `Variable<T>` | 单个变量（Name / Value / Guid），序列化列表元素 |
| `VariableContainer<T>` | 按类型（string / bool / int / float）的变量容器 |
| `VariableBundle` | **四类容器捆成一组**，统一泛型读写 `Get<T>` / `Set<T>` / `Has<T>` |
| `VariableBundleObject` | ScriptableObject 资产，Inspector 可直接编辑变量 |
| `PersistentVariableScope` | 枚举：`Room`（房间）/ `Global`（全局） |
| `PersistentVariableManager` | 持久变量管理器基类（订阅事件 + Import/Export） |
| `RoomVariableManager` | 房间变量单例（按场景从 `Resources/PersistentVariables/Room/` 加载） |
| `GameGlobalVariableManager` | 全局变量单例（手动配置当前/默认变量对象） |

### 3.2 dict vs list 语义（关键约定）

- `variableList`：**设计默认值**，编辑模式唯一数据源（Inspector 直接编辑）；
- `runtimeDict`：**运行时当前值**，有存档则从存档构建，否则从列表构建；
- 运行时 `Set` **只写 dict**；编辑模式（非运行）`Set` 才写列表。

> ⚠️ 因此运行时改变量不会污染资产默认值，存档读档才有意义。

### 3.3 使用

```csharp
RoomVariableManager.Instance.Set("hp", 100f);
float hp = RoomVariableManager.Instance.Get<float>("hp", 0f);

GameGlobalVariableManager.Instance.Set("playerName", "小明");
```

### 3.4 命名约定

- 房间变量资产命名 = **场景名**：`PersistentVariables/Room/<场景名>.asset`，找不到时编辑器自动创建。

---

## 4. 存档系统（Save）

> 位置：`Assets/Scripts/Base/Save`

持久化**节点图变量、房间变量、全局变量、物品状态**。

### 4.1 两层存档模型

- **staging（预备）**：离开房间时自动写入，保证房间进度不丢；
- **archive（正式）**：真正保存 = staging 覆盖 archive；**读档只读 archive**。

```
persistentDataPath/save/
├── index.json          # lastScene、saveTime
├── global.json         # 全局变量（仅真正保存时写）
├── staging/<房间>.json # 预备
└── archive/<房间>.json # 正式
```

### 4.2 核心类

| 类 | 说明 |
|---|---|
| `RoomSaveData` | 单房间存档：局部变量 + 图变量（按图 GUID）+ 物品状态 |
| `GlobalSaveData` | 全局变量 |
| `SaveIndex` | 存档元数据 |
| `SaveSystem` | 场景单例：`Commit` / `Load` / `Delete` / `ApplyRoomSave` |
| `PersistentItem` | 物品基类（持有 `VariableBundle` + `OnBeforeSave`/`OnAfterLoad` 钩子） |
| `PersistentItemManager` | 物品状态收集/分发单例（每房间） |

### 4.3 API

```csharp
SaveSystem.Commit();   // 真正保存（存档点 / Inspector 右键）
SaveSystem.Load();     // 读档（启动自动调用）
SaveSystem.Delete();   // 删除整个 save 目录
SaveSystem.HasSave();  // 是否存在正式存档
```

### 4.4 物品持久化

物品挂 `PersistentItem`，状态直接存 `Variables`（VariableBundle），移除逻辑由物品自己处理：

```csharp
public class MyItem : PersistentItem
{
    public override void OnAfterLoad()
    {
        if (Variables.Get<bool>("removed", false)) gameObject.SetActive(false);
    }
    public void Pickup()
    {
        Variables.Set("removed", true);
        gameObject.SetActive(false);
    }
}
```

> 存档位置：`C:\Users\<用户名>\AppData\LocalLow\DefaultCompany\GoTo2.5D\save\`
> 房间文件按场景分槽，只有【进入过并保存过】的房间才有存档。

---

## 5. 节点图系统（xNode）★ 核心

> 位置：`Assets/Scripts/Base/xNode`（xNode 框架本体在 `Assets/Plugins`）

基于 **xNode** 的可视化脚本系统。节点图资产（`BaseNodeGraph`）自带变量包（`VariableBundle`），
玩法逻辑 = **节点连接成的有向图**，由挂在场景物体上的执行器驱动。

### 5.1 结构

```
xNode/
├── BaseNodeGraph.cs              # 图资产（变量包 + 稳定 GUID）
├── GraphExecutor.cs              # 挂在场景物体上执行图（协程驱动）
└── Nodes/
    ├── BaseNode/                 # BaseNode / DataNode / FlowNode / StartNode / EndNode
    ├── VariableNodes/            # 本图变量 get/set（字符串/布尔/整数/浮点）
    ├── CommunicationNodes/       # 跨图通讯 + 执行图 + 存档点
    ├── ScopeVariableNode/        # 持久变量（房间/全局作用域）
    ├── UICommunicatorNodes/      # 跨图更新 UI 文本
    ├── BranchNodes/              # 分支 / 多分支 / 字符串条件
    ├── LogicNodes/               # 与 / 或 / 非
    ├── ValueNodes/               # 字符串/整数/布尔 常量值
    ├── TransformNodes/           # 移动 / 旋转物体
    └── OrderNodes/               # Print 等
```

### 5.2 节点基类与执行模型

```csharp
public abstract class BaseNode : Node
{
    public abstract BaseNode GetConnectedNode();  // 返回连接的下一个节点
    public abstract void Execute();               // 执行本节点逻辑
}
```

执行流程（`GraphExecutor.ExecuteCoroutine`）：

1. 每个 `executeInterval` 秒触发一轮；
2. 从 `startNode` 开始，沿 `GetConnectedNode()` 走链式执行（上限 100 次防死循环）；
3. 支持 `autoExecute`（启动即跑）、`executeCount`（0 = 无限循环）；
4. `executeInterval > 0` 时循环等待，是【每帧/定时驱动】的玩法逻辑载体。

### 5.3 节点分类与菜单

| 分类 | 菜单 | 说明 |
|---|---|---|
| 本图变量 | `变量操作/获取\|设置/字符串\|布尔\|整数\|浮点` | 读写本图的变量包 |
| 跨图通讯 | `通讯/获取\|设置/...` | 按目标图名跨图读写变量 |
| 持久变量 | `通讯/持久变量/获取\|设置/...` | 房间 / 全局作用域（枚举选择） |
| 执行图 | `通讯/执行节点图` | 触发另一张图执行 |
| 存档点 | `通讯/存档/保存游戏` | 把预备存档提交为正式存档 |

### 5.4 节点图 GUID（存档键）

- 每张图有稳定 `GUID`（`BaseNodeGraph.Guid`），**存档以 GUID 为键**；
- 新图首次保存后持久化；`[ContextMenu("重新生成GUID")]` 可手动更换（**会破坏存档对应关系**）。

### 5.5 一个具体节点示例

新增的 `GetFloatVarNode`（本图变量-获取浮点）就是极简继承写法：

```csharp
using UnityEngine;

/// <summary>
/// 变量操作-获取浮点变量节点（本图变量）
/// </summary>
[CreateNodeMenu("变量操作/获取/浮点")]
public class GetFloatVarNode : GetVarNode<float>
{
}
```

所有具体逻辑（读 `Graph` 的 `VariableBundle`、泛型取值、输出到连接端口）都在泛型基类
`GetVarNode<T>` / `SetVarNode<T>` 里完成——**这就是节点系统的扩展模式**：
新节点 = 继承基类 + 一个 `CreateNodeMenu` 特性，即可出现在编辑器菜单中。

### 5.6 事件支撑

通讯 / 持久变量节点通过 `ParameterizedEvent` 泛型事件与 `GraphCommunicator`、
`PersistentVariableManager` 交互；`GraphCommunicator` 启动时自动读档。

---

## 6. 音乐系统（Music）

> 位置：`Assets/Scripts/Base/Music`

`MusicManager` 单例，访问 `Instance` 时自动生成 GameObject 并 `DontDestroyOnLoad`：

| 方法 | 说明 |
|---|---|
| `PlayMusic(AudioClip, bool loop = true)` | 播放背景音乐 |
| `StopMusic()` | 停止音乐 |
| `PlaySFX(AudioClip)` | 播放音效 |
| `SetMusicVolume(float)` / `SetSFXVolume(float)` | 音量（0~1） |
| `PlayClickSFX()` | 点击音效（可扩展默认音效） |

---

## 7. UI 系统（UI）

> 位置：`Assets/Scripts/Base/UI` + `Base/xNode/Nodes/UICommunicatorNodes`

### 7.1 TextEvents

挂在 UI 文本（TextMeshProUGUI）上：

| 方法 | 说明 |
|---|---|
| `UpdateText(string)` | 直接设置文本 |
| `UpdateTextWithRichText(string)` | 用 `<wave>` 富文本包裹设置（TMPEffects 特效） |

### 7.2 UI 通讯节点（跨图更新 UI）

- `ComGetSelfTextNode` / `ComSetSelfTextNode`（Text）
- `ComGetSelfTextMeshProNode` / `ComSetSelfTextMeshProNode`（TMP）

由 `UICommunicator`（单例）按【图 + UI 名】缓存组件引用，节点图里可直接读写 UI 文本——
把 UI 更新也纳入了可视化玩法逻辑。

---

## 8. 场景与运行时流程

场景：`开始.unity`、`002.unity`（场景专用脚本在 `Scripts/Scene_开始`）。

典型运行时链路：

```
场景启动
 ├─ SaveSystem.Load()          # 启动自动读档（GraphCommunicator 亦读档）
 ├─ RoomVariableManager        # 按场景名加载房间变量资产
 ├─ GraphExecutor.Awake()      # 绑定图 → 注册到 GraphCommunicator
 └─ GraphExecutor.Start()      # autoExecute → 从 startNode 走节点链
      └─ 节点链执行（本图变量读写 / 跨图通讯 / 持久变量 / UI 更新 / 存档点）
```

---

## 9. 第三方依赖（Assets/Plugins）

| 插件 | 用途 |
|---|---|
| **xNode** | 可视化节点图框架（本体） |
| DOTween | 补间动画 |
| Koreographer | 音乐节奏同步 |
| TMPEffects | TextMeshPro 富文本特效（wave 等） |
| Editor / TMPEffects-main | 编辑器扩展、TMP 特效包 |

---

## 10. 设计约定速查

1. **玩法逻辑优先用节点图**，C# 只做地基与扩展节点；
2. **变量四类型**（string/bool/int/float），通过 `VariableBundle` 统一泛型读写，不支持其他类型；
3. **列表是默认值、字典是运行值**：运行时 `Set` 不写资产；
4. **存档两层**：staging 自动防丢、archive 是正式档、读档只看 archive；
5. **图 GUID 是存档键**：不要随意重新生成；
6. **物品状态 = VariableBundle**，持久化走 `PersistentItem` + `PersistentItemManager`；
7. **事件同步发布 + 对象池**，事件字段记得在 `OnRecycled` 清理；
8. **扩展节点 = 继承基类 + `[CreateNodeMenu]`**（如 `GetFloatVarNode : GetVarNode<float>`）。

---

*文档由 DeepSeek Harness 依据源码与子系统 README 整理*