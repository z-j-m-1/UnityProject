# GoTo2.5D 工程架构说明

> 2.5D 俯视角游戏工程（Unity **2022.3.62f3c1**）· 版本控制：Git
> 本文档随代码演进持续维护，具体 API 以源码为准。

---

## 1. 工程总览

GoTo2.5D 以**可视化节点图（xNode）驱动玩法逻辑**：玩法逻辑尽量不进 C# 代码，
而是在节点图资产里拖出来；C# 侧只提供【地基】——事件总线、变量存储、存档、执行器、场景行为、UI 收集。

```
事件系统(Event)        ← 泛型事件总线，支撑通讯 / 持久变量节点
变量系统(Variable)     ← 数据地基：节点图/房间/全局变量、物品状态共用
    └─ 存档系统(Save)  → 两层存档（staging/archive），持久化变量与物品
节点图系统(xNode)      ← 可视化玩法逻辑（变量读写/跨图通讯/持久变量/UI/存档点）
```

### 顶层目录结构

```
To2.5D/                        # Git 仓库根
├── README.md                  # 项目说明（系统索引）
├── 架构说明.md                 # 本文档
└── GoTo2.5D/                  # Unity 工程根
    ├── Assets/
    │   ├── Scenes/            # 002.unity、开始.unity
    │   ├── Scripts/
    │   │   ├── Base/          # 核心地基（6 大系统）
    │   │   │   ├── Event/     # 事件总线
    │   │   │   ├── Variable/  # 变量系统（含 RoomIdentity、Editor 工具）
    │   │   │   ├── Save/      # 存档系统
    │   │   │   ├── Music/     # 音乐系统
    │   │   │   ├── UI/        # UI 系统（UICollector / TextEvents）
    │   │   │   └── xNode/     # 节点图系统
    │   │   └── Scene_开始/    # 开始场景专用脚本（UI / Graph）
    │   ├── Plugins/           # 第三方插件（xNode/DOTween/Koreographer/TMPEffects）
    │   ├── Resources/         # 运行时加载资源
    │   │   └── PersistentVariables/Room/   # 房间变量资产（按场景名）
    │   └── TextMesh Pro/      # TMP 资源
    ├── ProjectSettings/       # 工程设置
    └── Packages/              # 包清单（URP 等）
```

---

## 2. 事件系统（Event）

> 位置：`Assets/Scripts/Base/Event` · 命名空间 `ZGameFramework.Core`

轻量级**泛型事件总线**，带对象池回收，用于解耦各系统（尤其节点图通讯）。

| 类 | 说明 |
|---|---|
| `EventBus` | 静态事件总线（发布 / 订阅 / 退订） |
| `GameEvent` | 事件基类，实现 `IPoolable` |
| `ParameterizedEvent<T>` | 泛型参数事件：`Trigger` / `TriggerAsync` / `TriggerAsyncRecycle` / `Subscribe` / `Unsubscribe` |
| `SignalEvent<T>` | 无参信号事件 |
| `ClassPool<T>` / `ListPool<T>` | 对象池（`IPoolable` 配合） |

**关键语义**：

- `Trigger` **同步发布**且事件对象从池复用，回调返回后自动回收；实现 `OnRecycled()` 清理字段防脏数据；
- 异步高频事件用 `TriggerAsyncRecycle`（不自动回收），需外部手动 `ClassPool<T>.Recycle`。

```csharp
public class MyEvent : ParameterizedEvent<MyEvent>
{
    public int value;
    public override void OnRecycled() { value = 0; }
}
MyEvent.Subscribe(e => Debug.Log(e.value));
MyEvent.Trigger(e => e.value = 42);   // 同步派发，自动回收
```

---

## 3. 变量系统（Variable）

> 位置：`Assets/Scripts/Base/Variable`

**统一变量存储体系**：节点图变量、房间变量、全局变量、物品状态共用同一套机制。

### 3.1 核心类

| 类 | 说明 |
|---|---|
| `Variable<T>` | 单个变量（Name / Value / Guid），序列化列表元素，GUID 自动生成 |
| `VariableContainer<T>` | 按类型（string / bool / int / float）的变量容器，含名字↔GUID 映射 |
| `VariableBundle` | 四类容器捆成一组，统一泛型读写 `Get<T>` / `Set<T>` / `Has<T>` / `TryResolve<T>` |
| `VariableBundleObject` | ScriptableObject 资产，Inspector 可直接编辑变量 |
| `PersistentVariableScope` | 枚举：`Room`（房间）/ `Global`（全局） |
| `PersistentVariableManager` | 持久变量管理器基类（订阅事件 + Import/Export） |
| `RoomVariableManager` | 房间变量单例（优先 RoomIdentity 资产，其次按场景名） |
| `GameGlobalVariableManager` | 全局变量单例（手动配置当前/默认变量对象） |
| `RoomIdentity` | 房间身份组件（roomId + 直接引用变量资产），抗场景改名 |

### 3.2 名字 ↔ GUID 解析（抗改名）

全模式 **名字优先 + GUID 兜底**：改名后旧引用靠 GUID 命中，GUID 变化后靠新名字命中。

- `VariableContainer<T>.TryResolve(name, guid, out value, out actualName, out actualGuid)` / `TryResolveAndSet(...)`
- `VariableBundle` / `VariableBundleObject` / `PersistentVariableManager` / `BaseNodeGraph` 均暴露同名泛型方法
- Get/Set 节点面板显示 `variableGuid`，运行时自动记录并在命中后回填修正（双向适配）
- 场景侧：`RoomIdentity` 直接引用房间变量资产（按资产 GUID 绑定），场景文件改名不影响加载

### 3.3 dict vs list 语义（关键约定）

- `variableList`：**设计默认值**，编辑模式唯一数据源（Inspector 直接编辑）；
- `runtimeDict`：**运行时当前值**，有存档则从存档构建，否则从列表构建；
- 运行时 `Set` **只写 dict**；编辑模式（非运行）`Set` 才写列表；
- `persist=false` 的变量**不进存档、读档不覆盖**，每次开始游戏重置为初始值。

> ⚠️ 因此运行时改变量不会污染资产默认值，存档读档才有意义。

### 3.4 使用

```csharp
RoomVariableManager.Instance.Set("hp", 100f);
float hp = RoomVariableManager.Instance.Get<float>("hp", 0f);

GameGlobalVariableManager.Instance.Set("playerName", "小明");
string name = GameGlobalVariableManager.Instance.Get<string>("playerName", "");

// 名字优先 + GUID 兜底解析
if (BaseNodeGraph.TryGetVariable("hp", guid, out float v, out string actualName, out string actualGuid))
{
    // ...
}
```

### 3.5 RoomIdentity（场景身份）

挂在房间场景根物体上，提供 `roomId`（自动生成、随场景持久化）+ 房间变量资产直接引用。
`RoomVariableManager` 加载流程：**①RoomIdentity 资产引用 → ②按场景名 Resources 加载 → ③编辑器自动创建**。
编辑器提供菜单 **Tools/房间/RoomIdentity 补齐（当前场景 / Build Settings 所有场景）**：
自动创建 RoomIdentity 根物体、生成 roomId、创建/回填变量资产并保存场景（仅编辑模式可用，播放中置灰）。

---

## 4. 存档系统（Save）

> 位置：`Assets/Scripts/Base/Save`

### 4.1 两层存档

- **staging（预备）**：离开房间时自动写入，保证房间进度不丢；
- **archive（正式）**：真正保存 = staging 覆盖 archive；**读档只读 archive**。

```
persistentDataPath/save/
├── index.json          # lastScene、saveTime
├── global.json         # 全局变量（仅真正保存时写）
├── staging/<房间>.json # 预备
└── archive/<房间>.json # 正式
```

### 4.2 核心类

| 类 | 说明 |
|---|---|
| `RoomSaveData` | 单房间存档：局部变量 + 图变量（按图GUID）+ 物品状态 + `roomId` |
| `GlobalSaveData` | 全局变量 |
| `SaveIndex` | 存档元数据 |
| `SaveSystem` | 场景单例：`Commit` / `Load` / `Delete` / `HasSave` / `ApplyRoomSave` |
| `PersistentItem` | 物品基类（持有 VariableBundle + `OnBeforeSave`/`OnAfterLoad` 钩子） |
| `PersistentItemManager` | 物品状态收集/分发单例 |

### 4.3 存档格式（v2，含 GUID）

v2 起变量条目为 `VariableEntryData<T>`（`name` + `guid` + `value`），变量改名后读档仍能靠 GUID 兜底命中。
**旧 v1 存档不兼容**，可直接删除 `save/` 目录重置。

- `RoomSaveData.roomId`：记录房间稳定 ID，场景改名后按场景名找不到文件时扫描 archive 按 roomId 兜底匹配；
- 房间文件名仍用场景名（`archive/<场景名>.json`），便于人工查看。

### 4.4 API

```csharp
SaveSystem.Commit();   // 真正保存（存档点 / Inspector 右键）
SaveSystem.Load();     // 读档（启动自动调用）
SaveSystem.Delete();   // 删除整个 save 目录
SaveSystem.HasSave();  // 是否存在正式存档
```

---

## 5. 节点图系统（xNode）

> 位置：`Assets/Scripts/Base/xNode`

基于 xNode 的可视化脚本系统。图资产（`BaseNodeGraph`）自带变量包（`VariableBundle`）与稳定 `GUID`。

### 5.1 目录结构

```
xNode/
├── BaseNodeGraph.cs       # 图资产（变量包 + 图GUID + TryGetVariable/TrySetVariable）
├── GraphExecutor.cs       # 挂在场景物体上执行图
└── Nodes/
    ├── BaseNode/          # BaseNode / DataNode / FlowNode / StartNode / EndNode / EntryNode
    ├── BranchNodes/       # 分支（Branch / MultiBranch / StringCondition）
    ├── LogicNodes/        # 逻辑（And / Or / No）+ 比较（Compare）
    ├── MathNodes/         # 数学运算（四则运算 / 比较）
    ├── OrderNodes/        # 流程（Print / Wait / 等待条件）
    ├── StringNodes/       # 字符串运算（运算 / 比较 / 长度）
    ├── ValueNodes/        # 常量值（Bool / Int / String）
    ├── TransformNodes/    # 物体变换（Move / Rote，继承 ComponentActionNode）
    ├── CommunicationNodes/# 通讯（GraphCommunicator + 事件 + 执行图 + 存档点）
    ├── UICommunicatorNodes# UI 通讯（ComUIGetTextNode / ComUISetTextNode）
    └── VariableNodes/     # 统一 get/set 变量节点（source 枚举）
```

### 5.2 统一 get/set 变量节点

一个获取基类（`GetVariableNode<T>`）+ 一个设置基类（`SetVariableNode<T>`），
通过 **`VariableSource` 枚举**选择操作对象：

| source | 含义 |
|---|---|
| `Self` | 本图变量 |
| `ExternalGraph` | 跨图通讯（按目标图名） |
| `Room` / `Global` | 房间 / 全局持久变量 |

- 端口保持强类型（`T`）；加新类型只需新建 2 个节点文件（Get + Set）；
- 节点面板显示 `variableGuid`（自动记录，方便调试）；**名字优先 + GUID 兜底**解析，命中后自动回填修正；
- 具体节点：`GetVariableBool/Int/Float/StringNode`、`SetVariableBool/Int/Float/StringNode`。

### 5.3 其他节点

| 分类 | 节点 | 说明 |
|---|---|---|
| UI 通讯 | `ComUIGetTextNode` / `ComUISetTextNode` | 读/写 Text / TextMeshPro（source：自身或 Canvas） |
| 执行图 | `ComExecutionGraphNode` | 触发另一张图执行 |
| 存档点 | `ComSaveGameNode` | 把预备存档提交为正式存档 |
| 变换 | `MoveObjectNode` / `RoteObjectNode` | 移动 / 旋转（目标：图绑定物体 / 子物体名 / 直接引用） |
| 分支/逻辑/值/变换 | `BranchNode`、`AndLogicNode`、`BoolValueNode` 等 | 流程控制、常量、物体运动 |
| 数学运算 | `MathOpIntNode` / `MathOpFloatNode` / `CompareIntNode` / `CompareFloatNode` | 四则运算（加/减/乘/除）、比较（等于/大于/小于等，输出 bool） |
| 字符串 | `StringOpNode` / `StringCompareNode` / `StringLengthNode` | 拼接/大小写/去空格、包含/开头/结尾比较（输出 bool）、长度 |
| 流程 | `PrintNode` / `WaitNode` / `WaitUntilNode` | 日志输出 / 等待指定秒数 / 等待条件成立（可接比较·逻辑·变量节点，支持超时） |

### 5.4 执行流程

`GraphExecutor` 挂在场景物体上（字段：`nodeGraph` / `autoExecute` / `executeInterval` / `executeCount`）：

1. `Awake`：绑定图到挂载物体、注册到 `GraphCommunicator`；
2. `Start`：`autoExecute` 时启动协程；
3. 每 `executeInterval` 秒从起点沿 `GetConnectedNode()` 走链式执行（上限 100 次防死循环）；
4. `executeCount`（0 = 无限循环）；`[ContextMenu("执行节点图")]` 可手动触发；
5. 执行起点可选 `Default`（startNode）或 `Entry`（按标识符/GUID 从入口节点开始，未找到则报错不执行）；
6. 执行游标为执行器私有（多执行器跑同一张图互不干扰）；
7. 触发策略 `triggerPolicy`：Restart / IgnoreWhileRunning / Queue。

### 5.5 节点图 GUID（存档键）

- 每张图有稳定 `GUID`（`BaseNodeGraph.Guid`），**存档以图 GUID 为键**；
- 新图首次保存后持久化；`[ContextMenu("重新生成GUID")]` 可手动更换（**会破坏存档对应关系**）。

### 5.6 扩展模式

新节点 = 继承基类 + `[CreateNodeMenu]` 特性：

```csharp
[CreateNodeMenu("变量操作/获取/浮点")]
public class GetVariableFloatNode : GetVariableNode<float> { }
```

---

## 6. UI 系统（UI）

> 位置：`Assets/Scripts/Base/UI` + `Base/xNode/Nodes/UICommunicatorNodes`

### 6.1 UICollector

挂在 Canvas 上，收集其下所有 `Text` / `TextMeshPro` / `Image`，按**物体名**索引。
单例，**不 DontDestroyOnLoad**（随场景重建）。

- 自动找场景 Canvas；没有则创建（补 `CanvasScaler` + `GraphicRaycaster`）；
- 同类型同名会覆盖并打警告；
- `Find<T>(name)` 按类型 + 名字查找；`Refresh()` 重新收集。

### 6.2 TextEvents

挂在 UI 文本（TextMeshProUGUI）上：

| 方法 | 说明 |
|---|---|
| `UpdateText(string)` | 直接设置文本 |
| `UpdateTextWithRichText(string)` | 用 `<wave>` 富文本包裹设置（TMPEffects） |

### 6.3 UI 通讯节点

`ComUIGetTextNode` / `ComUISetTextNode`：
- `source`：`Self`（图附加物体的自身/子物体）或 `Canvas`（UICollector 任意 UI）；
- `uiType`：`Text` / `TextMeshPro`（`Image` 只收集、暂不支持读写）；
- Get 节点带 `stripRichText` 选项（剔除 `<...>` 富文本标签）。

---

## 7. 音乐系统（Music）

> 位置：`Assets/Scripts/Base/Music`

`MusicManager` 单例，访问 `Instance` 时自动生成 GameObject 并 `DontDestroyOnLoad`：

| 方法 | 说明 |
|---|---|
| `PlayMusic(AudioClip, bool loop = true)` | 播放背景音乐 |
| `StopMusic()` | 停止音乐 |
| `PlaySFX(AudioClip)` | 播放音效 |
| `SetMusicVolume(float)` / `SetSFXVolume(float)` | 音量（0~1） |
| `PlayClickSFX()` | 点击音效（可扩展默认音效） |

---

## 8. 场景与运行时流程

场景：`开始.unity`、`002.unity`（场景专用脚本在 `Scripts/Scene_开始`）。

典型运行时链路：

```
场景启动
 ├─ SaveSystem.Load()          # 启动自动读档
 ├─ RoomVariableManager        # 优先 RoomIdentity 资产，其次按场景名加载
 ├─ GraphExecutor.Awake()      # 绑定图 → 注册到 GraphCommunicator
 └─ GraphExecutor.Start()      # autoExecute → 从 startNode 走节点链
      └─ 节点链执行（变量读写 / 跨图通讯 / 持久变量 / UI 更新 / 存档点）
```

---

## 9. 第三方依赖（Assets/Plugins）

| 插件 | 用途 |
|---|---|
| **xNode** | 可视化节点图框架（本体） |
| DOTween | 补间动画 |
| Koreographer | 音乐节奏同步 |
| TMPEffects | TextMeshPro 富文本特效（wave 等） |

---

## 10. 设计约定速查

1. **玩法逻辑优先用节点图**，C# 只做地基与扩展节点；
2. **变量四类型**（string/bool/int/float），通过 `VariableBundle` 统一泛型读写；
3. **列表是默认值、字典是运行值**：运行时 `Set` 不写资产；
4. **名字优先 + GUID 兜底**：变量 / 图 / 房间都有稳定 ID，改名不破坏引用与存档；
5. **存档两层**：staging 自动防丢、archive 是正式档、读档只看 archive；存档格式 v2（含 GUID）；
6. **图 GUID / roomId 是存档键**：不要随意重新生成；
7. **物品状态 = VariableBundle**，持久化走 `PersistentItem` + `PersistentItemManager`；
8. **事件同步发布 + 对象池**，事件字段记得在 `OnRecycled` 清理；
9. **扩展节点 = 继承基类 + `[CreateNodeMenu]`**；
10. **场景身份用 `RoomIdentity`**：挂组件 + 用编辑器工具一键补齐，Play 一次自动回填。
