# 节点清单（自动生成）

> 由 `Tools/节点系统/生成节点参考文档` 生成。新增/修改节点后请重新生成，勿手改。
> 约定：`input` / `next` 为流程通用端口，省略不列；`targetGameObject`（ComponentActionNode 目标端口）照列。

## 目录
- [值](#值)
- [其他](#其他)
- [分支条件](#分支条件)
- [列表](#列表)
- [刚体](#刚体)
- [动画](#动画)
- [参数](#参数)
- [取值](#取值)
- [变换](#变换)
- [变量操作](#变量操作)
- [基本](#基本)
- [子图](#子图)
- [字符串](#字符串)
- [插值](#插值)
- [数学运算](#数学运算)
- [流程](#流程)
- [流程分支](#流程分支)
- [物理](#物理)
- [状态机](#状态机)
- [生成](#生成)
- [相机](#相机)
- [通讯](#通讯)
- [通讯UI](#通讯UI)
- [逻辑门](#逻辑门)
- [音频](#音频)

## 值

### 值/三维向量（Vector3ValueNode）
- 基类：ValueNode`1 → DataNode → BaseNode
- 端口：
  - 输出 `Value`（Vector3）

### 值/二维向量（Vector2ValueNode）
- 基类：ValueNode`1 → DataNode → BaseNode
- 端口：
  - 输出 `Value`（Vector2）

### 值/字符串（StringValueNode）
- 基类：ValueNode`1 → DataNode → BaseNode
- 端口：
  - 输出 `Value`（String）

### 值/布尔（BoolValueNode）
- 基类：ValueNode`1 → DataNode → BaseNode
- 端口：
  - 输出 `Value`（Boolean）

### 值/整形（IntValueNode）
- 基类：ValueNode`1 → DataNode → BaseNode
- 端口：
  - 输出 `Value`（Int32）

### 值/浮点（FloatValueNode）
- 基类：ValueNode`1 → DataNode → BaseNode
- 端口：
  - 输出 `Value`（Single）

## 其他

### 其他/打印（PrintNode）
- 基类：FlowNode → BaseNode
- 颜色：`#FF00B3`
- 端口：
  - 输入 `printMessage`（String）

## 分支条件

### 分支条件/字符条件（StringConditionNode）
- 基类：DataNode → BaseNode
- 摘要：<summary> 分支条件-字符条件节点（数据节点）：对输入句子与预设文本做比较，输出 bool[] </summary>
- 颜色：`#E67E22`
- 端口：
  - 输出 `conditions`（Boolean[]）
  - 输入 `sentence`（String）
- 字段：
  - `text`（String[]）= 空

## 列表

### 列表/取元素/Vector2（ListGetItemVector2Node）
- 基类：ListGetItemNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-取元素-二维向量</summary>
- 端口：
  - 输入 `list`（List<Vector2>）
  - 输入 `index`（Int32）
  - 输出 `item`（Vector2）

### 列表/取元素/Vector3（ListGetItemVector3Node）
- 基类：ListGetItemNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-取元素-三维向量</summary>
- 端口：
  - 输入 `list`（List<Vector3>）
  - 输入 `index`（Int32）
  - 输出 `item`（Vector3）

### 列表/取元素/字符串（ListGetItemStringNode）
- 基类：ListGetItemNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-取元素-字符串</summary>
- 端口：
  - 输入 `list`（List<String>）
  - 输入 `index`（Int32）
  - 输出 `item`（String）

### 列表/取元素/整数（ListGetItemIntNode）
- 基类：ListGetItemNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-取元素-整数</summary>
- 端口：
  - 输入 `list`（List<Int32>）
  - 输入 `index`（Int32）
  - 输出 `item`（Int32）

### 列表/取元素/浮点（ListGetItemFloatNode）
- 基类：ListGetItemNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-取元素-浮点</summary>
- 端口：
  - 输入 `list`（List<Single>）
  - 输入 `index`（Int32）
  - 输出 `item`（Single）

### 列表/数量/Vector2（ListCountVector2Node）
- 基类：ListCountNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-数量-二维向量</summary>
- 端口：
  - 输入 `list`（List<Vector2>）
  - 输出 `count`（Int32）

### 列表/数量/Vector3（ListCountVector3Node）
- 基类：ListCountNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-数量-三维向量</summary>
- 端口：
  - 输入 `list`（List<Vector3>）
  - 输出 `count`（Int32）

### 列表/数量/字符串（ListCountStringNode）
- 基类：ListCountNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-数量-字符串</summary>
- 端口：
  - 输入 `list`（List<String>）
  - 输出 `count`（Int32）

### 列表/数量/整数（ListCountIntNode）
- 基类：ListCountNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-数量-整数</summary>
- 端口：
  - 输入 `list`（List<Int32>）
  - 输出 `count`（Int32）

### 列表/数量/浮点（ListCountFloatNode）
- 基类：ListCountNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-数量-浮点</summary>
- 端口：
  - 输入 `list`（List<Single>）
  - 输出 `count`（Int32）

### 列表/是否包含/Vector2（ListContainsVector2Node）
- 基类：ListContainsNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-是否包含-二维向量</summary>
- 端口：
  - 输入 `list`（List<Vector2>）
  - 输入 `item`（Vector2）
  - 输出 `contains`（Boolean）

### 列表/是否包含/Vector3（ListContainsVector3Node）
- 基类：ListContainsNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-是否包含-三维向量</summary>
- 端口：
  - 输入 `list`（List<Vector3>）
  - 输入 `item`（Vector3）
  - 输出 `contains`（Boolean）

### 列表/是否包含/字符串（ListContainsStringNode）
- 基类：ListContainsNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-是否包含-字符串</summary>
- 端口：
  - 输入 `list`（List<String>）
  - 输入 `item`（String）
  - 输出 `contains`（Boolean）

### 列表/是否包含/整数（ListContainsIntNode）
- 基类：ListContainsNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-是否包含-整数</summary>
- 端口：
  - 输入 `list`（List<Int32>）
  - 输入 `item`（Int32）
  - 输出 `contains`（Boolean）

### 列表/是否包含/浮点（ListContainsFloatNode）
- 基类：ListContainsNode`1 → DataNode → BaseNode
- 摘要：<summary>列表-是否包含-浮点</summary>
- 端口：
  - 输入 `list`（List<Single>）
  - 输入 `item`（Single）
  - 输出 `contains`（Boolean）

### 列表/添加/Vector2（ListAddVector2Node）
- 基类：ListAddNode`1 → FlowNode → BaseNode
- 摘要：<summary>列表-添加元素-二维向量</summary>
- 端口：
  - 输入 `list`（List<Vector2>）
  - 输入 `item`（Vector2）

### 列表/添加/Vector3（ListAddVector3Node）
- 基类：ListAddNode`1 → FlowNode → BaseNode
- 摘要：<summary>列表-添加元素-三维向量</summary>
- 端口：
  - 输入 `list`（List<Vector3>）
  - 输入 `item`（Vector3）

### 列表/添加/字符串（ListAddStringNode）
- 基类：ListAddNode`1 → FlowNode → BaseNode
- 摘要：<summary>列表-添加元素-字符串</summary>
- 端口：
  - 输入 `list`（List<String>）
  - 输入 `item`（String）

### 列表/添加/整数（ListAddIntNode）
- 基类：ListAddNode`1 → FlowNode → BaseNode
- 摘要：<summary>列表-添加元素-整数</summary>
- 端口：
  - 输入 `list`（List<Int32>）
  - 输入 `item`（Int32）

### 列表/添加/浮点（ListAddFloatNode）
- 基类：ListAddNode`1 → FlowNode → BaseNode
- 摘要：<summary>列表-添加元素-浮点</summary>
- 端口：
  - 输入 `list`（List<Single>）
  - 输入 `item`（Single）

### 列表/移除/Vector2（ListRemoveVector2Node）
- 基类：ListRemoveNode`1 → FlowNode → BaseNode
- 摘要：<summary>列表-移除元素-二维向量</summary>
- 端口：
  - 输入 `list`（List<Vector2>）
  - 输入 `item`（Vector2）

### 列表/移除/Vector3（ListRemoveVector3Node）
- 基类：ListRemoveNode`1 → FlowNode → BaseNode
- 摘要：<summary>列表-移除元素-三维向量</summary>
- 端口：
  - 输入 `list`（List<Vector3>）
  - 输入 `item`（Vector3）

### 列表/移除/字符串（ListRemoveStringNode）
- 基类：ListRemoveNode`1 → FlowNode → BaseNode
- 摘要：<summary>列表-移除元素-字符串</summary>
- 端口：
  - 输入 `list`（List<String>）
  - 输入 `item`（String）

### 列表/移除/整数（ListRemoveIntNode）
- 基类：ListRemoveNode`1 → FlowNode → BaseNode
- 摘要：<summary>列表-移除元素-整数</summary>
- 端口：
  - 输入 `list`（List<Int32>）
  - 输入 `item`（Int32）

### 列表/移除/浮点（ListRemoveFloatNode）
- 基类：ListRemoveNode`1 → FlowNode → BaseNode
- 摘要：<summary>列表-移除元素-浮点</summary>
- 端口：
  - 输入 `list`（List<Single>）
  - 输入 `item`（Single）

## 刚体

### 刚体/施加力（RigidbodyAddForceNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>刚体-施加力（3D，世界空间牛顿力；目标解析沿用 ComponentActionNode 的输入端口 / 目标模式）</summary>
- 颜色：`#FF8844`
- 端口：
  - 输入 `force`（Vector3）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `forceMode`（ForceMode）= Force
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 刚体/施加力(2D)（Rigidbody2DAddForceNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>刚体-施加力（2D，世界空间牛顿力；目标解析沿用 ComponentActionNode 的输入端口 / 目标模式）</summary>
- 颜色：`#FF8844`
- 端口：
  - 输入 `force`（Vector2）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `forceMode`（ForceMode2D）= Force
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 刚体/设置角速度（RigidbodySetAngularVelocityNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>刚体-设置角速度（3D，世界空间；目标解析沿用 ComponentActionNode 的输入端口 / 目标模式）</summary>
- 颜色：`#FF8844`
- 端口：
  - 输入 `angularVelocity`（Vector3）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 刚体/设置速度（RigidbodySetVelocityNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>刚体-设置速度（3D，世界空间；目标解析沿用 ComponentActionNode 的输入端口 / 目标模式）</summary>
- 颜色：`#FF8844`
- 端口：
  - 输入 `velocity`（Vector3）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 刚体/设置速度(2D)（Rigidbody2DSetVelocityNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>刚体-设置速度（2D，世界空间；目标解析沿用 ComponentActionNode 的输入端口 / 目标模式）</summary>
- 颜色：`#FF8844`
- 端口：
  - 输入 `velocity`（Vector2）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

## 动画

### 动画/交叉淡入（CrossFadeAnimatorNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>动画-交叉淡入（Animator.CrossFade）</summary>
- 端口：
  - 输入 `stateName`（String）
  - 输入 `transitionDuration`（Single）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 动画/布尔参数（SetAnimatorBoolNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>动画-布尔参数（Animator.SetBool）</summary>
- 端口：
  - 输入 `paramName`（String）
  - 输入 `value`（Boolean）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 动画/播放（PlayAnimationNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>动画-播放（Animator）</summary>
- 端口：
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `stateName`（String）= 空
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 动画/整数参数（SetAnimatorIntNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>动画-整数参数（Animator.SetInteger）</summary>
- 端口：
  - 输入 `paramName`（String）
  - 输入 `value`（Int32）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 动画/浮点参数（SetAnimatorFloatNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>动画-浮点参数（Animator.SetFloat）</summary>
- 端口：
  - 输入 `paramName`（String）
  - 输入 `value`（Single）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 动画/触发参数（SetAnimatorTriggerNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>动画-触发参数（Animator.SetTrigger）</summary>
- 端口：
  - 输入 `triggerName`（String）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

## 参数

### 参数/输入/三维向量（SubGraphInputVector3Node）
- 基类：SubGraphInputNode`1 → SubGraphInputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输入-三维向量</summary>
- 端口：
  - 输出 `value`（Vector3）
- 字段：
  - `parameterName`（String）= 空

### 参数/输入/三维向量列表（SubGraphInputVector3ListNode）
- 基类：SubGraphInputListNode`1 → SubGraphInputNode`1 → SubGraphInputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输入-三维向量列表</summary>
- 端口：
  - 输出 `value`（List<Vector3>）
- 字段：
  - `parameterName`（String）= 空

### 参数/输入/二维向量（SubGraphInputVector2Node）
- 基类：SubGraphInputNode`1 → SubGraphInputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输入-二维向量</summary>
- 端口：
  - 输出 `value`（Vector2）
- 字段：
  - `parameterName`（String）= 空

### 参数/输入/二维向量列表（SubGraphInputVector2ListNode）
- 基类：SubGraphInputListNode`1 → SubGraphInputNode`1 → SubGraphInputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输入-二维向量列表</summary>
- 端口：
  - 输出 `value`（List<Vector2>）
- 字段：
  - `parameterName`（String）= 空

### 参数/输入/字符串（SubGraphInputStringNode）
- 基类：SubGraphInputNode`1 → SubGraphInputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输入-字符串</summary>
- 端口：
  - 输出 `value`（String）
- 字段：
  - `parameterName`（String）= 空

### 参数/输入/字符串列表（SubGraphInputStringListNode）
- 基类：SubGraphInputListNode`1 → SubGraphInputNode`1 → SubGraphInputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输入-字符串列表</summary>
- 端口：
  - 输出 `value`（List<String>）
- 字段：
  - `parameterName`（String）= 空

### 参数/输入/布尔（SubGraphInputBoolNode）
- 基类：SubGraphInputNode`1 → SubGraphInputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输入-布尔</summary>
- 端口：
  - 输出 `value`（Boolean）
- 字段：
  - `parameterName`（String）= 空

### 参数/输入/整数（SubGraphInputIntNode）
- 基类：SubGraphInputNode`1 → SubGraphInputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输入-整数</summary>
- 端口：
  - 输出 `value`（Int32）
- 字段：
  - `parameterName`（String）= 空

### 参数/输入/整数列表（SubGraphInputIntListNode）
- 基类：SubGraphInputListNode`1 → SubGraphInputNode`1 → SubGraphInputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输入-整数列表</summary>
- 端口：
  - 输出 `value`（List<Int32>）
- 字段：
  - `parameterName`（String）= 空

### 参数/输入/浮点（SubGraphInputFloatNode）
- 基类：SubGraphInputNode`1 → SubGraphInputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输入-浮点</summary>
- 端口：
  - 输出 `value`（Single）
- 字段：
  - `parameterName`（String）= 空

### 参数/输入/浮点列表（SubGraphInputFloatListNode）
- 基类：SubGraphInputListNode`1 → SubGraphInputNode`1 → SubGraphInputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输入-浮点列表</summary>
- 端口：
  - 输出 `value`（List<Single>）
- 字段：
  - `parameterName`（String）= 空

### 参数/输入/物体（SubGraphInputGameObjectNode）
- 基类：SubGraphInputNodeBase → DataNode → BaseNode
- 摘要：<summary> 子图参数输入-物体（取值源，不序列化）：父图 SubGraphNode 执行前把连线值写入本节点 value 字段， 子图内部连本节点输出端口取目标物体。不走变量系统（GameObject 不入 VariableBundle 序列化）。 </summary>
- 端口：
  - 输出 `value`（GameObject）
- 字段：
  - `parameterName`（String）= 空

### 参数/输出/三维向量（SubGraphOutputVector3Node）
- 基类：SubGraphOutputNode`1 → SubGraphOutputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输出-三维向量</summary>
- 端口：
  - 输入 `value`（Vector3）
- 字段：
  - `parameterName`（String）= 空

### 参数/输出/三维向量列表（SubGraphOutputVector3ListNode）
- 基类：SubGraphOutputListNode`1 → SubGraphOutputNode`1 → SubGraphOutputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输出-三维向量列表</summary>
- 端口：
  - 输入 `value`（List<Vector3>）
- 字段：
  - `parameterName`（String）= 空

### 参数/输出/二维向量（SubGraphOutputVector2Node）
- 基类：SubGraphOutputNode`1 → SubGraphOutputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输出-二维向量</summary>
- 端口：
  - 输入 `value`（Vector2）
- 字段：
  - `parameterName`（String）= 空

### 参数/输出/二维向量列表（SubGraphOutputVector2ListNode）
- 基类：SubGraphOutputListNode`1 → SubGraphOutputNode`1 → SubGraphOutputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输出-二维向量列表</summary>
- 端口：
  - 输入 `value`（List<Vector2>）
- 字段：
  - `parameterName`（String）= 空

### 参数/输出/字符串（SubGraphOutputStringNode）
- 基类：SubGraphOutputNode`1 → SubGraphOutputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输出-字符串</summary>
- 端口：
  - 输入 `value`（String）
- 字段：
  - `parameterName`（String）= 空

### 参数/输出/字符串列表（SubGraphOutputStringListNode）
- 基类：SubGraphOutputListNode`1 → SubGraphOutputNode`1 → SubGraphOutputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输出-字符串列表</summary>
- 端口：
  - 输入 `value`（List<String>）
- 字段：
  - `parameterName`（String）= 空

### 参数/输出/布尔（SubGraphOutputBoolNode）
- 基类：SubGraphOutputNode`1 → SubGraphOutputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输出-布尔</summary>
- 端口：
  - 输入 `value`（Boolean）
- 字段：
  - `parameterName`（String）= 空

### 参数/输出/整数（SubGraphOutputIntNode）
- 基类：SubGraphOutputNode`1 → SubGraphOutputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输出-整数</summary>
- 端口：
  - 输入 `value`（Int32）
- 字段：
  - `parameterName`（String）= 空

### 参数/输出/整数列表（SubGraphOutputIntListNode）
- 基类：SubGraphOutputListNode`1 → SubGraphOutputNode`1 → SubGraphOutputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输出-整数列表</summary>
- 端口：
  - 输入 `value`（List<Int32>）
- 字段：
  - `parameterName`（String）= 空

### 参数/输出/浮点（SubGraphOutputFloatNode）
- 基类：SubGraphOutputNode`1 → SubGraphOutputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输出-浮点</summary>
- 端口：
  - 输入 `value`（Single）
- 字段：
  - `parameterName`（String）= 空

### 参数/输出/浮点列表（SubGraphOutputFloatListNode）
- 基类：SubGraphOutputListNode`1 → SubGraphOutputNode`1 → SubGraphOutputNodeBase → DataNode → BaseNode
- 摘要：<summary>子图参数输出-浮点列表</summary>
- 端口：
  - 输入 `value`（List<Single>）
- 字段：
  - `parameterName`（String）= 空

### 参数/输出/物体（SubGraphOutputGameObjectNode）
- 基类：SubGraphOutputNodeBase → DataNode → BaseNode
- 摘要：<summary> 子图参数输出-物体（返回值槽，不序列化）：子图内部把目标物体连到本节点输入端口， 子图链跑完后父图求值读回。不走变量系统（GameObject 不入 VariableBundle 序列化）。 </summary>
- 端口：
  - 输入 `value`（GameObject）
- 字段：
  - `parameterName`（String）= 空

## 取值

### 取值/获取物体（GetGameObjectNode）
- 基类：DataNode → BaseNode
- 摘要：<summary> 取值-获取物体：把目标物体作为数据输出， 供操作节点（继承 ComponentActionNode）的 GameObject 输入端口接线。 无序列化场景引用，运行时求值。 </summary>
- 端口：
  - 输入 `objectName`（String）
  - 输出 `output`（GameObject）
- 字段：
  - `source`（GameObjectSource）= Self

### 取值/转换/三维向量(三个浮点)（Float3ToVector3Node）
- 基类：DataNode → BaseNode
- 摘要：<summary>取值-转换：三个浮点合成一个三维向量（输入可接线或节点上填值）</summary>
- 端口：
  - 输入 `x`（Single）
  - 输入 `y`（Single）
  - 输入 `z`（Single）
  - 输出 `vector`（Vector3）

### 取值/转换/三维向量(二维向量)（Vector2ToVector3Node）
- 基类：DataNode → BaseNode
- 摘要：<summary>取值-转换：二维向量合成三维向量（z = 0，输入可接线或节点上填值）</summary>
- 端口：
  - 输入 `vector2`（Vector2）
  - 输出 `vector3`（Vector3）

### 取值/转换/二维向量(三维向量)（Vector3ToVector2Node）
- 基类：DataNode → BaseNode
- 摘要：<summary>取值-转换：三维向量取 x/y 成二维向量（z 丢弃，输入可接线或节点上填值）</summary>
- 端口：
  - 输入 `vector3`（Vector3）
  - 输出 `vector2`（Vector2）

### 取值/转换/二维向量(两个浮点)（Float2ToVector2Node）
- 基类：DataNode → BaseNode
- 摘要：<summary>取值-转换：两个浮点合成一个二维向量（输入可接线或节点上填值）</summary>
- 端口：
  - 输入 `x`（Single）
  - 输入 `y`（Single）
  - 输出 `vector`（Vector2）

### 取值/转换/字符串(整数)（IntToStringNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>取值-转换：整数 → 字符串</summary>
- 端口：
  - 输出 `result`（String）

### 取值/转换/字符串(浮点)（FloatToStringNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>取值-转换：浮点 → 字符串</summary>
- 端口：
  - 输出 `result`（String）

### 取值/转换/整数(字符串)（StringToIntNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>取值-转换：字符串 → 整数（失败返回 0）</summary>
- 端口：
  - 输出 `result`（Int32）

### 取值/转换/整数(浮点)（FloatToIntNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>取值-转换：浮点 → 整数（四舍五入）</summary>
- 端口：
  - 输出 `result`（Int32）

### 取值/转换/浮点(字符串)（StringToFloatNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>取值-转换：字符串 → 浮点（失败返回 0）</summary>
- 端口：
  - 输出 `result`（Single）

### 取值/转换/浮点(整数)（IntToFloatNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>取值-转换：整数 → 浮点</summary>
- 端口：
  - 输出 `result`（Single）

## 变换

### 变换/旋转（RoteObjectNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>变换-旋转物体节点（对目标 Transform 增加旋转偏移）</summary>
- 颜色：`#44AAFF`
- 端口：
  - 输入 `roteOffset`（Vector3）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 变换/移动（MoveObjectNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>变换-移动物体节点（对目标 Transform 增加位移偏移）</summary>
- 颜色：`#44AAFF`
- 端口：
  - 输入 `moveOffset`（Vector3）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 变换/缩放（ScaleObjectNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>变换-缩放物体（目标 Transform 增加缩放偏移）</summary>
- 颜色：`#44AAFF`
- 端口：
  - 输入 `scaleOffset`（Vector3）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 变换/设置位置（SetPositionNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>变换-设置物体位置</summary>
- 颜色：`#44AAFF`
- 端口：
  - 输入 `targetPosition`（Vector3）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 变换/设置旋转（SetRotationNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>变换-设置物体旋转（欧拉角）</summary>
- 颜色：`#44AAFF`
- 端口：
  - 输入 `rotation`（Vector3）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

## 变量操作

### 变量操作/获取/Vector2（GetVariableVector2Node）
- 基类：GetVariableNode`1 → GetVariableNodeBase → DataNode → BaseNode
- 摘要：<summary>获取变量-二维向量（source 枚举选择本图/跨图/房间/全局）</summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输出 `outputValue`（Vector2）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空
  - `defaultValue`（Vector2）= (0, 0)

### 变量操作/获取/Vector2列表（GetVariableVector2ListNode）
- 基类：GetVariableNode`1 → GetVariableNodeBase → DataNode → BaseNode
- 摘要：<summary>获取列表变量-二维向量（source 枚举选择本图/跨图/房间/全局）</summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输出 `outputValue`（List<Vector2>）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空
  - `defaultValue`（List<Vector2>）= 空

### 变量操作/获取/Vector3（GetVariableVector3Node）
- 基类：GetVariableNode`1 → GetVariableNodeBase → DataNode → BaseNode
- 摘要：<summary>获取变量-Vector3（source 枚举选择本图/跨图/房间/全局）</summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输出 `outputValue`（Vector3）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空
  - `defaultValue`（Vector3）= (0, 0, 0)

### 变量操作/获取/Vector3列表（GetVariableVector3ListNode）
- 基类：GetVariableNode`1 → GetVariableNodeBase → DataNode → BaseNode
- 摘要：<summary>获取列表变量-三维向量（source 枚举选择本图/跨图/房间/全局）</summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输出 `outputValue`（List<Vector3>）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空
  - `defaultValue`（List<Vector3>）= 空

### 变量操作/获取/字符串（GetVariableStringNode）
- 基类：GetVariableNode`1 → GetVariableNodeBase → DataNode → BaseNode
- 摘要：<summary> 变量操作-获取字符串变量节点（本图/跨图/房间/全局） </summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输出 `outputValue`（String）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空
  - `defaultValue`（String）= 空

### 变量操作/获取/字符串列表（GetVariableStringListNode）
- 基类：GetVariableNode`1 → GetVariableNodeBase → DataNode → BaseNode
- 摘要：<summary>获取列表变量-字符串（source 枚举选择本图/跨图/房间/全局）</summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输出 `outputValue`（List<String>）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空
  - `defaultValue`（List<String>）= 空

### 变量操作/获取/布尔（GetVariableBoolNode）
- 基类：GetVariableNode`1 → GetVariableNodeBase → DataNode → BaseNode
- 摘要：<summary> 变量操作-获取布尔变量节点（本图/跨图/房间/全局） </summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输出 `outputValue`（Boolean）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空
  - `defaultValue`（Boolean）= false

### 变量操作/获取/整数（GetVariableIntNode）
- 基类：GetVariableNode`1 → GetVariableNodeBase → DataNode → BaseNode
- 摘要：<summary> 变量操作-获取整数变量节点（本图/跨图/房间/全局） </summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输出 `outputValue`（Int32）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空
  - `defaultValue`（Int32）= 0

### 变量操作/获取/整数列表（GetVariableIntListNode）
- 基类：GetVariableNode`1 → GetVariableNodeBase → DataNode → BaseNode
- 摘要：<summary>获取列表变量-整数（source 枚举选择本图/跨图/房间/全局）</summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输出 `outputValue`（List<Int32>）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空
  - `defaultValue`（List<Int32>）= 空

### 变量操作/获取/浮点（GetVariableFloatNode）
- 基类：GetVariableNode`1 → GetVariableNodeBase → DataNode → BaseNode
- 摘要：<summary> 变量操作-获取浮点变量节点（本图/跨图/房间/全局） </summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输出 `outputValue`（Single）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空
  - `defaultValue`（Single）= 0

### 变量操作/获取/浮点列表（GetVariableFloatListNode）
- 基类：GetVariableNode`1 → GetVariableNodeBase → DataNode → BaseNode
- 摘要：<summary>获取列表变量-浮点（source 枚举选择本图/跨图/房间/全局）</summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输出 `outputValue`（List<Single>）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空
  - `defaultValue`（List<Single>）= 空

### 变量操作/设置/Vector2（SetVariableVector2Node）
- 基类：SetVariableNode`1 → SetVariableNodeBase → FlowNode → BaseNode
- 摘要：<summary>设置变量-二维向量（source 枚举选择本图/跨图/房间/全局）</summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输入 `variableValue`（Vector2）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空

### 变量操作/设置/Vector2列表（SetVariableVector2ListNode）
- 基类：SetVariableNode`1 → SetVariableNodeBase → FlowNode → BaseNode
- 摘要：<summary>设置列表变量-二维向量（source 枚举选择本图/跨图/房间/全局）</summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输入 `variableValue`（List<Vector2>）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空

### 变量操作/设置/Vector3（SetVariableVector3Node）
- 基类：SetVariableNode`1 → SetVariableNodeBase → FlowNode → BaseNode
- 摘要：<summary>设置变量-Vector3（source 枚举选择本图/跨图/房间/全局）</summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输入 `variableValue`（Vector3）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空

### 变量操作/设置/Vector3列表（SetVariableVector3ListNode）
- 基类：SetVariableNode`1 → SetVariableNodeBase → FlowNode → BaseNode
- 摘要：<summary>设置列表变量-三维向量（source 枚举选择本图/跨图/房间/全局）</summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输入 `variableValue`（List<Vector3>）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空

### 变量操作/设置/字符串（SetVariableStringNode）
- 基类：SetVariableNode`1 → SetVariableNodeBase → FlowNode → BaseNode
- 摘要：<summary> 变量操作-设置字符串变量节点（本图/跨图/房间/全局） </summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输入 `variableValue`（String）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空

### 变量操作/设置/字符串列表（SetVariableStringListNode）
- 基类：SetVariableNode`1 → SetVariableNodeBase → FlowNode → BaseNode
- 摘要：<summary>设置列表变量-字符串（source 枚举选择本图/跨图/房间/全局）</summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输入 `variableValue`（List<String>）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空

### 变量操作/设置/布尔（SetVariableBoolNode）
- 基类：SetVariableNode`1 → SetVariableNodeBase → FlowNode → BaseNode
- 摘要：<summary> 变量操作-设置布尔变量节点（本图/跨图/房间/全局） </summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输入 `variableValue`（Boolean）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空

### 变量操作/设置/整数（SetVariableIntNode）
- 基类：SetVariableNode`1 → SetVariableNodeBase → FlowNode → BaseNode
- 摘要：<summary> 变量操作-设置整数变量节点（本图/跨图/房间/全局） </summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输入 `variableValue`（Int32）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空

### 变量操作/设置/整数列表（SetVariableIntListNode）
- 基类：SetVariableNode`1 → SetVariableNodeBase → FlowNode → BaseNode
- 摘要：<summary>设置列表变量-整数（source 枚举选择本图/跨图/房间/全局）</summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输入 `variableValue`（List<Int32>）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空

### 变量操作/设置/浮点（SetVariableFloatNode）
- 基类：SetVariableNode`1 → SetVariableNodeBase → FlowNode → BaseNode
- 摘要：<summary> 变量操作-设置浮点变量节点（本图/跨图/房间/全局） </summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输入 `variableValue`（Single）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空

### 变量操作/设置/浮点列表（SetVariableFloatListNode）
- 基类：SetVariableNode`1 → SetVariableNodeBase → FlowNode → BaseNode
- 摘要：<summary>设置列表变量-浮点（source 枚举选择本图/跨图/房间/全局）</summary>
- 端口：
  - 输入 `targetName`（String）
  - 输入 `variableName`（String）
  - 输入 `variableValue`（List<Single>）
- 字段：
  - `source`（VariableSource）= Self
  - `variableGuid`（String）= 空

## 基本

### 基本/入口（EntryNode）
- 基类：BaseNode
- 摘要：<summary> 入口节点 - 与 StartNode 类似，但带字符串标识符，同一张图可放多个 GraphExecutor 选择"入口执行"时按标识符 / GUID 从该节点开始执行 </summary>
- 颜色：`#AA77DD`
- 字段：
  - `identifier`（String）= 空
  - `guid`（String）= 空

### 基本/开始（StartNode）
- 基类：BaseNode
- 颜色：`#44AA44`

### 基本/结束（EndNode）
- 基类：BaseNode
- 端口：
  - 输入 `Node`（流程）

## 子图

### 子图/执行（SubGraphNode）
- 基类：FlowNode → BaseNode
- 摘要：<summary> 子图执行节点：调用另一张节点图作为子流程。 - 动态端口自动与子图「参数输入/参数输出」节点同步（端口名 = 参数名）； - 执行时：父图连线值 → 子图变量 → 跑子图链 → 输出节点输入求值 → 输出端口； - 嵌套有深度上限（防循环引用）。 </summary>
- 颜色：`#8844CC`
- 字段：
  - `subGraph`（BaseNodeGraph）= 空
  - `entryIdentifier`（String）= 空
  - `resetVariablesOnCall`（Boolean）= false

## 字符串

### 字符串/截取（StringSubstringNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>字符串-截取（start 起 length 个字符；越界安全）</summary>
- 端口：
  - 输入 `start`（Int32）
  - 输入 `length`（Int32）
  - 输出 `result`（String）

### 字符串/替换（StringReplaceNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>字符串-替换（把 oldValue 全部替换为 newValue）</summary>
- 端口：
  - 输入 `oldValue`（String）
  - 输入 `newValue`（String）
  - 输出 `result`（String）

### 字符串/比较（StringCompareNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>字符串比较节点（等于/包含/以…开头/以…结尾，输出 bool），纯数据节点</summary>
- 端口：
  - 输入 `a`（String）
  - 输入 `b`（String）
  - 输出 `result`（Boolean）
- 字段：
  - `operation`（StringCompareOperation）= Equals

### 字符串/运算（StringOpNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>字符串运算节点（拼接/转大写/转小写/去空格），纯数据节点</summary>
- 端口：
  - 输入 `a`（String）
  - 输入 `b`（String）
  - 输出 `result`（String）
- 字段：
  - `operation`（StringOperation）= Concat

### 字符串/长度（StringLengthNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>字符串长度节点（输出 int），纯数据节点</summary>
- 端口：
  - 输出 `length`（Int32）

## 插值

### 插值/移动到（MoveToNode）
- 基类：ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary> 插值-移动到：目标 Transform 从当前位置在 duration 秒内插值到 targetPosition（GetFlow 逐帧，结束精确归位）。 目标解析复用 ComponentActionNodeBase（GameObject 输入端口 &gt; Attached/ByName/Direct）。 </summary>
- 颜色：`#44AAFF`
- 端口：
  - 输入 `targetPosition`（Vector3）
  - 输入 `duration`（Single）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 插值/透明度（FadeCanvasGroupNode）
- 基类：ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary> 插值-透明度：CanvasGroup.alpha 在 duration 秒内渐变到 targetAlpha（0=隐 1=显，逐帧插值，结束精确归位）。 目标解析复用 ComponentActionNodeBase（GameObject 输入端口 &gt; Attached/ByName/Direct）。 </summary>
- 颜色：`#44AAFF`
- 端口：
  - 输入 `targetAlpha`（Single）
  - 输入 `duration`（Single）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

## 数学运算

### 数学运算/二维向量缩放（Vector2ScaleNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>数学运算-二维向量缩放：向量 × 标量（输入可接线或节点上填值）</summary>
- 端口：
  - 输入 `vector`（Vector2）
  - 输入 `scalar`（Single）
  - 输出 `result`（Vector2）

### 数学运算/二维向量运算（Vector2OpNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>数学运算-二维向量：加 / 减（输入可接线或节点上填值）</summary>
- 端口：
  - 输入 `a`（Vector2）
  - 输入 `b`（Vector2）
  - 输出 `result`（Vector2）
- 字段：
  - `operation`（MathOperation）= Add

### 数学运算/整数比较（CompareIntNode）
- 基类：CompareNode`1 → DataNode → BaseNode
- 摘要：<summary>数学运算-整数比较（等于/不等/大于/小于等，输出 bool）</summary>
- 端口：
  - 输入 `a`（Int32）
  - 输入 `b`（Int32）
  - 输出 `result`（Boolean）
- 字段：
  - `operation`（CompareOperation）= Greater

### 数学运算/整数运算（MathOpIntNode）
- 基类：MathOpNode`1 → DataNode → BaseNode
- 摘要：<summary>数学运算-整数（加/减/乘/除）</summary>
- 端口：
  - 输入 `a`（Int32）
  - 输入 `b`（Int32）
  - 输出 `result`（Int32）
- 字段：
  - `operation`（MathOperation）= Add

### 数学运算/浮点比较（CompareFloatNode）
- 基类：CompareNode`1 → DataNode → BaseNode
- 摘要：<summary>数学运算-浮点比较（等于/不等/大于/小于等，输出 bool）</summary>
- 端口：
  - 输入 `a`（Single）
  - 输入 `b`（Single）
  - 输出 `result`（Boolean）
- 字段：
  - `operation`（CompareOperation）= Greater

### 数学运算/浮点运算（MathOpFloatNode）
- 基类：MathOpNode`1 → DataNode → BaseNode
- 摘要：<summary>数学运算-浮点（加/减/乘/除）</summary>
- 端口：
  - 输入 `a`（Single）
  - 输入 `b`（Single）
  - 输出 `result`（Single）
- 字段：
  - `operation`（MathOperation）= Add

### 数学运算/随机整数（RandomIntNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>数学运算-随机整数（含上限）</summary>
- 端口：
  - 输入 `min`（Int32）
  - 输入 `max`（Int32）
  - 输出 `result`（Int32）

### 数学运算/随机浮点（RandomFloatNode）
- 基类：DataNode → BaseNode
- 摘要：<summary>数学运算-随机浮点</summary>
- 端口：
  - 输入 `min`（Single）
  - 输入 `max`（Single）
  - 输出 `result`（Single）

## 流程

### 流程/并行（ParallelNode）
- 基类：FlowNode → BaseNode
- 摘要：<summary> 流程-并行：同时启动最多 4 条分支链（branchA–D，各自以 EndNode 收尾），全部完成后沿 next 继续。 并行链在宿主（执行器/状态机）协程上运行，真正交错执行；未连接的分支跳过。 </summary>
- 颜色：`#44CC88`
- 端口：
  - 输出 `branchA`（流程）
  - 输出 `branchB`（流程）
  - 输出 `branchC`（流程）
  - 输出 `branchD`（流程）

### 流程/条件循环（WhileLoopNode）
- 基类：FlowNode → BaseNode
- 摘要：<summary> 流程-条件循环：每轮先检查 condition，为 true 则执行循环体（body 子链，以 EndNode 收尾）， 之后回到条件判断；condition 为 false 时沿 next 继续。 maxIterations 防死循环（条件可由循环体改的变量驱动，也可能永远为 true）。 </summary>
- 颜色：`#44CC88`
- 端口：
  - 输入 `condition`（Boolean）
  - 输出 `body`（流程）
- 字段：
  - `maxIterations`（Int32）= 1000

### 流程/等待（WaitNode）
- 基类：FlowNode → BaseNode
- 摘要：<summary> 流程-等待节点：暂停执行链指定秒数（协程方式，不阻塞进程） </summary>
- 字段：
  - `duration`（Single）= 1

### 流程/等待条件（WaitUntilNode）
- 基类：ConditionWaitNode → FlowNode → BaseNode
- 摘要：<summary> 条件等待-通用：等待 condition 输入端口为 true 可由比较 / 逻辑 / 变量等数据节点组合出任意条件 </summary>
- 端口：
  - 输入 `condition`（Boolean）
- 字段：
  - `timeout`（Single）= 0

### 流程/计数循环（ForLoopNode）
- 基类：FlowNode → BaseNode
- 摘要：<summary> 流程-计数循环：从 startIndex 开始执行 count 次循环体（body 子链，以 EndNode 收尾）， 每轮把当前索引写到 index 输出端口；循环结束后沿 next 继续。 说明：并发跑同一张图时 index 会被互相覆盖（与其他共享节点状态一致）。 </summary>
- 颜色：`#44CC88`
- 端口：
  - 输入 `startIndex`（Int32）
  - 输入 `count`（Int32）
  - 输出 `index`（Int32）
  - 输出 `body`（流程）

### 流程/计时器（TimerNode）
- 基类：FlowNode → BaseNode
- 摘要：<summary> 流程-计时器：每隔 interval 秒执行一次 tick 子链（以 EndNode 收尾），共执行 times 次（0 = 无限循环）； 次数到后沿 next 继续。无限循环时靠停止链/切换状态来终止。 </summary>
- 颜色：`#44CC88`
- 端口：
  - 输入 `interval`（Single）
  - 输入 `times`（Int32）
  - 输出 `tick`（流程）

### 流程/跳转到入口（JumpToEntryNode）
- 基类：FlowNode → BaseNode
- 摘要：<summary> 流程-跳转到入口：执行目标入口（entryId）开头的链，执行完后当前链结束（不再沿 next 继续）。 适合"出错重试 / 直接切到某段逻辑"；目标入口不存在时报错并直接结束当前链。 </summary>
- 颜色：`#44CC88`
- 端口：
  - 输入 `entryId`（String）

### 流程/遍历列表/Vector2（ForEachLoopVector2Node）
- 基类：ForEachLoopNode`1 → FlowNode → BaseNode
- 摘要：<summary>流程-遍历列表-二维向量</summary>
- 端口：
  - 输入 `list`（List<Vector2>）
  - 输出 `item`（Vector2）
  - 输出 `body`（流程）

### 流程/遍历列表/Vector3（ForEachLoopVector3Node）
- 基类：ForEachLoopNode`1 → FlowNode → BaseNode
- 摘要：<summary>流程-遍历列表-三维向量</summary>
- 端口：
  - 输入 `list`（List<Vector3>）
  - 输出 `item`（Vector3）
  - 输出 `body`（流程）

### 流程/遍历列表/字符串（ForEachLoopStringNode）
- 基类：ForEachLoopNode`1 → FlowNode → BaseNode
- 摘要：<summary>流程-遍历列表-字符串</summary>
- 端口：
  - 输入 `list`（List<String>）
  - 输出 `item`（String）
  - 输出 `body`（流程）

### 流程/遍历列表/整数（ForEachLoopIntNode）
- 基类：ForEachLoopNode`1 → FlowNode → BaseNode
- 摘要：<summary>流程-遍历列表-整数</summary>
- 端口：
  - 输入 `list`（List<Int32>）
  - 输出 `item`（Int32）
  - 输出 `body`（流程）

### 流程/遍历列表/浮点（ForEachLoopFloatNode）
- 基类：ForEachLoopNode`1 → FlowNode → BaseNode
- 摘要：<summary>流程-遍历列表-浮点</summary>
- 端口：
  - 输入 `list`（List<Single>）
  - 输出 `item`（Single）
  - 输出 `body`（流程）

## 流程分支

### 流程分支/双分支（BranchNode）
- 基类：FlowNode → BaseNode
- 端口：
  - 输入 `condition`（Boolean）
  - 输出 `falseTo`（流程）

### 流程分支/多分支（MultiBranchNode）
- 基类：FlowNode → BaseNode
- 端口：
  - 输入 `conditions`（Boolean[]）
  - 输出 `outputs`（BaseNode[]）

## 物理

### 物理/圆形检测(2D)（PhysicsOverlapCircleNode）
- 基类：DataNode → BaseNode
- 摘要：<summary> 物理-圆形检测（2D）：以 center 为中心 radius 半径做 OverlapCircle，输出命中数量与按索引取命中物体/位置。 同帧内多次读取共享同一次检测结果（帧缓存）。 </summary>
- 颜色：`#44BBAA`
- 端口：
  - 输入 `center`（Vector2）
  - 输入 `radius`（Single）
  - 输入 `layerMask`（LayerMask）
  - 输入 `index`（Int32）
  - 输出 `hitCount`（Int32）
  - 输出 `hitObject`（GameObject）
  - 输出 `hitPoint`（Vector2）

### 物理/射线检测（PhysicsRaycastNode）
- 基类：DataNode → BaseNode
- 摘要：<summary> 物理-射线检测（3D）：从 origin 沿 direction 发射射线，输出命中结果。 同帧内多次读取共享同一次检测结果（帧缓存）。 </summary>
- 颜色：`#44BBAA`
- 端口：
  - 输入 `origin`（Vector3）
  - 输入 `direction`（Vector3）
  - 输入 `distance`（Single）
  - 输入 `layerMask`（LayerMask）
  - 输出 `isHit`（Boolean）
  - 输出 `hitPoint`（Vector3）
  - 输出 `hitNormal`（Vector3）
  - 输出 `hitDistance`（Single）
  - 输出 `hitObject`（GameObject）

### 物理/射线检测(2D)（PhysicsRaycast2DNode）
- 基类：DataNode → BaseNode
- 摘要：<summary> 物理-射线检测（2D）：从 origin 沿 direction 发射 2D 射线，输出命中结果。 同帧内多次读取共享同一次检测结果（帧缓存）。 </summary>
- 颜色：`#44BBAA`
- 端口：
  - 输入 `origin`（Vector2）
  - 输入 `direction`（Vector2）
  - 输入 `distance`（Single）
  - 输入 `layerMask`（LayerMask）
  - 输出 `isHit`（Boolean）
  - 输出 `hitPoint`（Vector2）
  - 输出 `hitNormal`（Vector2）
  - 输出 `hitDistance`（Single）
  - 输出 `hitObject`（GameObject）

### 物理/球形检测（PhysicsOverlapSphereNode）
- 基类：DataNode → BaseNode
- 摘要：<summary> 物理-球形检测（3D）：以 center 为中心 radius 半径做 OverlapSphere，输出命中数量与按索引取命中物体/位置。 同帧内多次读取共享同一次检测结果（帧缓存）。 </summary>
- 颜色：`#44BBAA`
- 端口：
  - 输入 `center`（Vector3）
  - 输入 `radius`（Single）
  - 输入 `layerMask`（LayerMask）
  - 输入 `index`（Int32）
  - 输出 `hitCount`（Int32）
  - 输出 `hitObject`（GameObject）
  - 输出 `hitPoint`（Vector3）

## 状态机

### 状态机/切换（GraphStateTransitionNode）
- 基类：FlowNode → BaseNode
- 摘要：<summary> 状态机-切换状态节点：让目标 GraphStateMachine 切换到指定状态。 通常放在某个状态子图内部，作为该状态的流转出口。 </summary>
- 颜色：`#AA66CC`
- 端口：
  - 输入 `machineName`（String）
  - 输入 `targetState`（String）

## 生成

### 生成/生成物体（SpawnObjectNode）
- 基类：FlowNode → BaseNode
- 摘要：<summary> 生成-生成物体：实例化预制体。 - 位置：接线 > 节点上填写（非零）> 预制体原位置； - 父物体：可选 GameObject 输入端口（不序列化场景引用）； - 生成后的物体写到 spawned 输出端口（非序列化），可接后续操作节点 / 销毁节点。 </summary>
- 颜色：`#88CC44`
- 端口：
  - 输入 `spawnPosition`（Vector3）
  - 输入 `eulerAngles`（Vector3）
  - 输入 `parent`（GameObject）
  - 输出 `spawned`（GameObject）
- 字段：
  - `prefab`（GameObject）= 空

### 生成/销毁物体（DestroyObjectNode）
- 基类：FlowNode → BaseNode
- 摘要：<summary> 生成-销毁物体：目标 = GameObject 输入端口（优先）> 直接引用字段。 可延迟销毁（0 = 立即）。 </summary>
- 颜色：`#88CC44`
- 端口：
  - 输入 `target`（GameObject）
  - 输入 `delay`（Single）
- 字段：
  - `targetObject`（GameObject）= 空

## 相机

### 相机/噪声振幅（SetVcamNoiseNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>相机-噪声振幅：虚拟相机加/取 BasicMultiChannelPerlin 噪声并设置振幅（持续震屏/呼吸感）。振幅 0 复位。</summary>
- 颜色：`#FFAA44`
- 端口：
  - 输入 `amplitude`（Single）
  - 输入 `frequency`（Single）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 相机/目标组添加成员（TargetGroupAddMemberNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>相机-目标组添加成员：向 CinemachineTargetGroup 添加成员（多目标取景）。</summary>
- 颜色：`#FFAA44`
- 端口：
  - 输入 `member`（GameObject）
  - 输入 `weight`（Single）
  - 输入 `radius`（Single）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 相机/虚拟相机优先级（SetVcamPriorityNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>相机-虚拟相机优先级：调高优先级即切入该相机（CinemachineBrain 自动混合），设 0 切走。</summary>
- 颜色：`#FFAA44`
- 端口：
  - 输入 `priority`（Int32）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 相机/设置注视（SetVcamLookAtNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>相机-设置注视目标：vcam.LookAt = 目标（接「获取物体」/「参数/输入/物体」）。</summary>
- 颜色：`#FFAA44`
- 端口：
  - 输入 `lookAtTarget`（GameObject）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 相机/设置跟随（SetVcamFollowNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>相机-设置跟随目标：vcam.Follow = 目标（接「获取物体」/「参数/输入/物体」）。</summary>
- 颜色：`#FFAA44`
- 端口：
  - 输入 `followTarget`（GameObject）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 相机/轨道速度（SetDollySpeedNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>相机-轨道速度：CinemachineDollyCart 沿路径移动速度（过场用）。</summary>
- 颜色：`#FFAA44`
- 端口：
  - 输入 `speed`（Single）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 相机/震屏（CinemachineImpulseNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary> 相机-震屏：给 CinemachineImpulseSource 施加冲击（命中/爆炸等）。 位置口接上 → GenerateImpulseAt(pos, vel)；速度口接上 → GenerateImpulse(vel)；都未接 → GenerateImpulse()。 相机需有 CinemachineImpulseListener 才可见震动。 </summary>
- 颜色：`#FFAA44`
- 端口：
  - 输入 `position`（Vector3）
  - 输入 `velocity`（Vector3）
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

## 通讯

### 通讯/存档/保存游戏（ComSaveGameNode）
- 基类：FlowNode → BaseNode
- 摘要：<summary> 通讯-保存游戏节点（存档点）- 执行到该节点时自动保存当前状态 </summary>

### 通讯/执行节点图（ComExecutionGraphNode）
- 基类：FlowNode → BaseNode
- 摘要：<summary> 通讯-执行节点图节点 </summary>
- 端口：
  - 输入 `graphName`（String）

## 通讯UI

### 通讯UI/获取文本（ComUIGetTextNode）
- 基类：DataNode → BaseNode
- 摘要：<summary> 通讯UI-获取文本节点（Text / TextMeshPro） </summary>
- 端口：
  - 输入 `uiObjectName`（String）
  - 输出 `outputValue`（String）
- 字段：
  - `source`（UISource）= Self
  - `uiType`（UIType）= TextMeshPro
  - `stripRichText`（Boolean）= false
  - `defaultValue`（String）= ""

### 通讯UI/设置文本（ComUISetTextNode）
- 基类：FlowNode → BaseNode
- 摘要：<summary> 通讯UI-设置文本节点（Text / TextMeshPro） </summary>
- 端口：
  - 输入 `uiObjectName`（String）
  - 输入 `textValue`（String）
- 字段：
  - `source`（UISource）= Self
  - `uiType`（UIType）= TextMeshPro

## 逻辑门

### 逻辑门/与门（AndLogicNode）
- 基类：LogicNode → DataNode → BaseNode
- 端口：
  - 输入 `conditionOne`（Boolean）
  - 输入 `conditionTwo`（Boolean）
  - 输出 `result`（Boolean）

### 逻辑门/或门（OrLogicNode）
- 基类：LogicNode → DataNode → BaseNode
- 端口：
  - 输入 `conditionOne`（Boolean）
  - 输入 `conditionTwo`（Boolean）
  - 输出 `result`（Boolean）

### 逻辑门/非门（NoLogicNode）
- 基类：DataNode → BaseNode
- 端口：
  - 输入 `condition`（Boolean）
  - 输出 `result`（Boolean）

## 音频

### 音频/停止（StopAudioNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>音频-停止（AudioSource）</summary>
- 端口：
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

### 音频/播放（PlayAudioNode）
- 基类：ComponentActionNode`1 → ComponentActionNodeBase → FlowNode → BaseNode
- 摘要：<summary>音频-播放（AudioSource）</summary>
- 端口：
  - 输入 `targetGameObject`（GameObject）
- 字段：
  - `target`（ComponentTarget）= Attached
  - `targetName`（String）= 空
  - `targetObject`（GameObject）= 空

