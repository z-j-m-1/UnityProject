新增等待节点（WaitNode）+ 执行器协程钩子

- BaseNode 新增 GetFlow() 协程钩子（默认 null，零影响现有节点）
- GraphExecutor 链式执行在每个节点后 yield 节点协程（等待等可暂停链，不阻塞进程）
- 新增 WaitNode（流程/等待）：等待指定秒数后继续，为以后"等待条件/对话选项"预留同一机制

新增数学运算节点（四则运算 + 比较）

- MathOpIntNode / MathOpFloatNode：加/减/乘/除（含除零保护），菜单 数学运算/整数运算、浮点运算
- CompareIntNode / CompareFloatNode：等于/不等/大于/大于等于/小于/小于等于（输出 bool），菜单 数学运算/整数比较、浮点比较
- 新增 MathNodes 文件夹；比较节点放 LogicNodes

新增入口节点（EntryNode）+ 图执行模式

- 新增 EntryNode（基本/入口）：带标识符 + 自动 GUID，一张图可放多个
- BaseNodeGraph.GetEntryNode(id) / GetAllEntryNodes()：运行时/编辑器实时扫描 nodes
- GraphExecutor 新增执行模式：Default（startNode）/ Entry（按标识符或 GUID 从入口开始，未找到报错不执行）
- GraphExecutor 自定义 Inspector：入口下拉选择器 + 标识符改名自动回填（编辑模式）
- Get/Set/入口节点的 GUID 只在检查器显示，节点图视图节点体上隐藏（避免误触）

近期修复（存档 / 编辑器 / 节点）

- 存档：Commit 后清空预备层；场景改名兜底命中后删除旧名称存档
- 存档：拆出 SaveSystem.LoadGlobal（只加载全局变量），启动改由它调用，房间存档由进房间时应用，不再重复加载
- RoomIdentity 补齐工具：过滤失效 Build Settings 场景条目 + 单场景容错；场景已挂载 RoomIdentity、禁用失效 SampleScene 并登记 002
- 编辑器模式：UI 通讯节点（Canvas 源）只读扫描场景 UI（不创建、不修改场景），悬停提示不再报错
- StringConditionNode 改为 DataNode（纯数据节点）+ NodeTint 区分
- 开始场景图资产合并：ShowNameText / What 合并为通用节点图.asset

文档整理：重写架构说明与全部 README + Base/xNode 目录清理

- 重写仓库根《架构说明.md》《README.md》与 Event/Music/Save/UI/Variable/xNode 子系统 README
- 删除 xNode/Nodes 下重构遗留的 5 个空文件夹及其 meta
- PersistentVariableEvent.cs 移入 CommunicationNodes/（与 ComSetAndGetVariableEvent 并列）

变量/场景 GUID 追踪（抗改名）+ RoomIdentity

- VariableContainer 新增 nameToGuid/guidToName 映射 + TryResolve/TryResolveAndSet（名字优先 + GUID 兜底）
- 存档格式 v2：变量条目改为 name+guid+value（VariableEntryData），旧存档不兼容
- Get/Set 节点隐藏 variableGuid 字段，运行时自动记录/修正（名字↔GUID 双向适配）
- 新增 RoomIdentity 组件（roomId + 直接引用房间变量资产），场景改名不影响加载
- SaveSystem 房间存档记录 roomId，场景改名后按房间ID扫描 archive 兜底匹配

UI通讯节点统一（UISource/UIType枚举）+ 新增UICollector

- 新增 UICollector（Canvas 单例）：收集 Text/TextMeshPro/Image，按物体名索引，同名警告
- UI 节点统一为 ComUIGetTextNode / ComUISetTextNode（source=自身/Canvas，type=Text/TextMeshPro）
- Get 节点新增 stripRichText 富文本剔除选项
- 删除旧 UICommunicator + 旧事件 + 旧 4 个自身UI节点

统一get/set变量节点为单一基类（source枚举）

- 新增 VariableSource 枚举（本图/跨图/房间/全局）
- GetVariableNode<T> / SetVariableNode<T> 统一基类，按 source 分发
- 具体节点从 24 个收敛到 8 个，加新类型只需 2 个文件
- 删除旧的本图/跨图/作用域 get/set 节点（30 个文件）

新增物品持久化系统（PersistentItem + PersistentItemManager）

- 新增 PersistentItem 基类：直接持有 VariableBundle，itemId 自动生成 GUID
- 移除 OnSaveState/OnLoadState，保留 OnBeforeSave/OnAfterLoad 可选钩子
- PersistentItemManager 单例：收集/分发每房间物品状态
- SaveSystem 房间存档接入物品数据（CaptureRoom/ApplyRoomSaveNow）