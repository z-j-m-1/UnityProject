新增游戏逻辑节点：生成/销毁 + 物理查询 + 刚体控制

- 生成/销毁（生成/）：SpawnObjectNode 实例化预制体（位置/旋转可接线，未接线非零用填写值、否则用预制体原变换；可选父物体；spawned 输出端口非序列化）；DestroyObjectNode 销毁物体（GameObject 输入端口 > 直接引用；可延迟）
- 物理查询（物理/，DataNode，帧缓存同帧共享检测结果）：
  - 射线检测 / 射线检测(2D)：origin + direction + distance + LayerMask → isHit / hitPoint / hitNormal / hitDistance / hitObject
  - 球形检测 / 圆形检测(2D)：center + radius + LayerMask + index → hitCount / 按索引取 hitObject / hitPoint（越界警告）
- 刚体控制（刚体/，继承 ComponentActionNode，目标解析统一：GameObject 输入端口 > Attached/ByName/Direct）：
  - 施加力（ForceMode）/ 设置速度 / 设置角速度（3D）
  - 施加力（ForceMode2D）/ 设置速度（2D）
- 全部 11 个新节点位于 SpawnNodes / PhysicsNodes / RigidbodyNodes

新增数据层：Vector2 类型 + 列表变量（全栈打通）

- Vector2 全栈：变量容器（vector2Container）+ 存档（VariableBundleData.vector2s）+ Get/Set 变量节点（变量操作/获取|设置/Vector2）+ 子图参数（子图/参数输入|输出/二维向量）+ 常量节点（值/二维向量）+ 转换（两个浮点→二维向量、二维向量↔三维向量）+ 数学（二维向量运算加/减、标量缩放）
- 列表变量（List）：五种元素类型（字符串/整数/浮点/Vector2/Vector3），VariableBundle 新增 stringList/intList/floatList/vector2List/vector3List 容器，定义初始值在图资产 / VariableBundleObject Inspector
  - Get/Set 列表变量节点（变量操作/获取|设置/字符串列表 等，source 支持本图/跨图/房间/全局）
  - 子图列表参数输入/输出节点（子图/参数输入|输出/字符串列表 等，ParamType = List&lt;T&gt;，SyncParameterPorts 自动识别）
  - 列表操作节点（列表/添加、移除、取元素、数量、是否包含，各 5 种元素类型）
  - 引用语义：Get 列表返回变量引用，添加/移除直接修改即写回；null 列表操作警告并跳过
- 存档：VariableBundleData 新增 vector2s + 5 个列表字段（VariableEntryData&lt;List&lt;T&gt;&gt;）；旧存档缺字段由 ImportFrom null 防护安全跳过
- 持久变量：PersistentVariableManager 订阅新增 Vector2 + 5 列表事件通道（房间/全局变量可用）
- 子图注入：SubGraphNode.InjectInputs 新增 Vector2 + 5 列表分发（子图变量 Set）

新增 GameObject 引用支持 + 图状态机容器

- GetGameObjectNode（菜单「取值/获取物体」）：GameObjectSource 枚举 Self（图绑定物体/子物体，transform.Find）/ All（全场景按名字，含 inactive）；对象名称为可接线 string 输入端口；输出字段非序列化（运行时求值，规避场景引用写进图资产）
- SceneObjectFinder 缓存字典：惰性构建「名字 → 物体」O(1) 查表；场景加载/卸载、运行时启动、编辑器 Hierarchy 变更自动失效；未命中重扫一次兜底；重名取第一个并警告一次
- ComponentActionNode&lt;T&gt; 基类新增 [Input][NonSerialized] GameObject targetGameObject 端口：已连线优先取 GetInputValue，null/未连线回退既有 ResolveTarget() → 旧图（仅目标模式）零破坏；一处改动全部子类（变换/音频/动画）生效
- 子图 GameObject 参数：SubGraphInputGameObjectNode / SubGraphOutputGameObjectNode（非泛型，同名单文件）；绕过 VariableBundle（无 GameObject 容器）直接写字段/求值读回；SyncParameterPorts 自动识别（ParamType=GameObject），端口同步零改动
- GraphStateMachine（MonoBehaviour）：状态 = 子图（stateName + 子图 + 入口标识 + loop）；TransitionTo 停旧链起新链（复用 GraphChainRunner），host=状态机自身（Attached 目标 = 状态机物体）；subscribeEntries 可选订阅当前状态子图入口事件；initialState 自动进入
- GraphStateTransitionNode（菜单「状态机/切换」）：machineName（空 = 图绑定物体上查找）/ targetState 均为可接线 string 端口
- NodeExecuteContext.Current 类型放宽 GraphExecutor → MonoBehaviour（执行器/状态机都可作链宿主）；GraphChainRunner host 参数同步放宽

修复：PrintNode / NoLogicNode 读原始字段未取输入端口连线值

- PrintNode.Execute 原为 Debug.Log(printMessage)（直接读字段），接线后仍只输出默认值"打印信息"；改为 GetInputValue<string>(nameof(printMessage), printMessage)，已连线取上游数据、未连线用字段默认值
- 审计全部带 [Input] 的节点：NoLogicNode.GetValue 原为 !condition（同类问题），改为 !GetInputValue<bool>(nameof(condition), condition)
- 其余节点（逻辑/分支/字符串/转换/数学/UI/通讯等）均确认已正确使用 GetInputValue，无同类问题

修复：子图参数节点端口方向（对齐标准"函数边界"语义）

修复：子图参数节点端口方向（对齐标准"函数边界"语义）

- 参数输入节点改为**输出端口**（取值源）：子图内部连它的输出取参数值，未注入时返回节点字段默认值
- 参数输出节点改为**输入端口**（返回值槽）：子图内部把结果连进来，子图链跑完后 SubGraphNode 求值该输入读回（不再依赖"设置变量"中转）
- SubGraphOutputNodeBase 新增 EvaluateValue() 类型化求值访问器；SubGraphNode.ReadBackOutputs 按参数名找到子图输出节点并求值
- 注意：已创建的子图.asset 里的参数节点需重放/重连（端口方向变化）

修复：子图参数节点"No script asset"报错（具体节点拆分为同名单文件）

修复：子图参数节点"No script asset"报错（具体节点拆分为同名单文件）

- 根因：Unity 要求可实例化的 ScriptableObject 类文件名与类名一致；此前 10 个具体参数节点（5 输入 + 5 输出）合在 SubGraphInputNode.cs / SubGraphOutputNode.cs 中，创建时报 No script asset
- 拆分：两个基类文件只保留抽象基类（SubGraphInputNodeBase / SubGraphInputNode&lt;T&gt;、SubGraphOutputNodeBase / SubGraphOutputNode&lt;T&gt;）；10 个具体节点各自独立成同名单文件（SubGraphInputStringNode.cs 等），[CreateNodeMenu] 一并迁移
- 运行时逻辑（动态端口同步/注入/回读）不变

子图封装（SubGraphNode + 参数端口）

子图封装（SubGraphNode + 参数端口）

- 新增 SubGraphNode（菜单「子图/执行」）：把另一张节点图当子流程调用；动态端口与子图「参数输入/输出」节点自动同步（端口名 = 参数名，类型严格匹配）
- 新增子图参数节点：SubGraphInputNode / SubGraphOutputNode（各 5 种类型：字符串/布尔/整数/浮点/三维向量），参数名 = 子图变量名，子图内部用现有「获取/设置变量」读写
- 抽取 GraphChainRunner 共享链执行器：主链与子图链同逻辑（Execute + yield 流程 + 循环上限），GraphExecutor 重构为调用它（行为不变）
- 执行语义：同步阻塞（父链等子图完成）；目标解析沿用父执行器；嵌套深度上限 8 防循环
- 编辑器：SubGraphNodeBodyEditor（子图资产下拉 / 入口下拉 / 循环引用与参数重名校验）
- 注意：多链并发调用同一子图时变量共享（需隔离复制资产）；resetVariablesOnCall 可重置子图变量；参数改名会断开对应端口连线

编辑器体验：节点浏览器（搜索/最近/收藏）+ 变量引用下拉

编辑器体验：节点浏览器（搜索/最近/收藏）+ 变量引用下拉

- 新增 NodeBrowserWindow：Tools/节点系统/节点浏览器，或节点图空白处右键「打开节点浏览器…」；按菜单路径/类名搜索、最近使用（自动记录最近 12 种）、★ 收藏（EditorPrefs 持久化）；点击即把节点加入当前打开的图（落点 = 打开位置）
- 新增 BaseNodeGraphEditor（[CustomNodeGraphEditor]）：右键菜单顶部插入「★ 收藏」「最近使用」快捷项 + 浏览器入口；重写 CreateNode 自动记入最近使用（任意方式创建节点都记录）
- 变量引用下拉：Get/Set 变量节点节点体的「变量名」改为下拉选择图中已定义变量（VariableBundle.GetAllVariableNames 全类型去重，BaseNodeGraph 透传），未命中保留手动输入
- 新增 NodeBrowserPrefs（EditorPrefs 持久化 + 节点类型↔菜单路径工具）

新增浮点常量节点（补全取值常量类型）

新增浮点常量节点（补全取值常量类型）

- FloatValueNode（菜单「值/浮点」）：与 Bool/Int/String/Vector3 常量对齐，位于 ValueNodes/Constants/，输出 float 可直接接入 Float3ToVector3Node / 数学运算 / 变量节点等

新增三维向量数据节点 + 变换节点 Vector3 端口输入 + 取值/变量节点目录分类

新增三维向量数据节点 + 变换节点 Vector3 端口输入 + 取值/变量节点目录分类

- 新增 Float3ToVector3Node（DataNode）：三个浮点输入（x/y/z，可接线）合成 Vector3 输出，菜单「取值/转换/三维向量(三个浮点)」
- 变换节点（Move/Rote/Scale/SetPosition/SetRotation，继承 ComponentActionNode<Transform>）的 Vector3 字段全部升级为输入端口 [Input]：位置/移动偏移/旋转/缩放都可接线输入（未接线时仍用节点上的默认值），可接 Vector3ValueNode / GetVariableVector3Node / Float3ToVector3Node
- 转换节点菜单前栏改为"目标类型"：浮点转整数→「整数(浮点)」、浮点转字符串→「字符串(浮点)」、整数转浮点→「浮点(整数)」、整数转字符串→「字符串(整数)」、字符串转浮点→「浮点(字符串)」、字符串转整数→「整数(字符串)」
- 目录分类：ValueNodes 拆分 Constants/（常量节点）与 Conversion/（转换节点）；VariableNodes 拆分 Get/（获取节点）与 Set/（设置节点），VariableSource 保留在根目录；文件与 .meta 一并移动（GUID 不变，图资产/场景引用不受影响）

修复：触发器入口标识下拉仅在已命中时才显示

修复：触发器入口标识下拉仅在已命中时才显示

- DrawEventIdPicker 重构：入口下拉**始终可见**（未命中时首项为「（选择入口标识…）」占位，选中真实入口才回填 eventId，不覆盖手填值）
- 未命中警告保留并区分两种情况：eventId 为空（引导从下拉选择）/ 非空未匹配（提示重新选择或修正输入）
- 数据源维持「场景中的 GraphExecutor 及其节点图」：发射器只应指向场景中实际存在的图入口

触发器入口标识下拉选择器（GraphEventEmitter / CollisionEventEmitter）

触发器入口标识下拉选择器（GraphEventEmitter / CollisionEventEmitter）

- 新增 GraphEventEntryOptionPicker 静态帮助类：扫描场景中 GraphExecutor 使用的节点图及入口，生成入口标识下拉选项（按图去重，文案带图名·节点名）
- GraphEventEmitter / CollisionEventEmitter 新增自定义 Inspector：eventId 命中入口 → 下拉选中；未命中 → 警告；始终保留手动输入（eventId 也可给非入口监听器用）
- 场景切换 / 层级变更 / 资源变更自动刷新列表，另提供「刷新入口列表」按钮兜底
- 复用方式：其他触发器 CustomEditor 调 GraphEventEntryOptionPicker.CollectEntryOptions() + DrawEventIdPicker() 即可

执行器多链并发 + 入口事件订阅模式（GraphExecutor 重构）

执行器多链并发 + 入口事件订阅模式（GraphExecutor 重构）

- GraphExecutor 重写为多链并发模型：每次事件/触发启动一条独立协程（互不打断），每条链独立游标（当前节点/执行轮数/排队标记）
- 触发策略按同一起点生效（不同入口互不影响）：Restart（停旧链重跑）/ IgnoreWhileRunning（忽略）/ Queue（当前链跑完再补跑一轮）
- 入口事件订阅 bool 升级为 EntryEventSubscribeMode 枚举：Off / CurrentEntry（只订阅自己入口）/ AllEntries（按事件标识解析对应入口并发执行）
- RunningNode 改为 RunningNodes（多链同时高亮），NodeRunHighlight 同步支持
- 共享图变量保留：并发链需要合作共用变量，需要独立各建变量
- 新增 GraphExecutor.README.md：枚举 / 触发策略 / 并发模型 / 配置示例专项说明；xNode/README 执行流程同步更新

事件触发节点（复用入口节点 + GraphEvent 事件总线）

事件触发节点（复用入口节点 + GraphEvent 事件总线）

- 新增 GraphEvent（ParameterizedEvent）：字符串 eventId 事件总线，任意触发源统一调用
- GraphExecutor 新增 subscribeEntryEvent 开关：入口模式开启后订阅该入口标识的事件，触发时从该入口执行（ExecuteFrom）
- Execute 重构为 ExecuteFrom(BaseNode start)：事件/指定起点优先，触发策略（Restart/Ignore/Queue）同样生效
- 新增 GraphEventEmitter（Emit()，可拖入按钮/UnityEvent，不引用节点图）+ CollisionEventEmitter（2D/3D 碰撞触发）
- 事件入口 = 入口节点（identifier 即事件标识），不新建节点类型

数据节点补全 + 组件动作节点扩库存 + Vector3 变量类型

- 数据节点：RandomIntNode / RandomFloatNode（随机）、6 个类型转换节点（int↔float↔string）、StringSubstringNode / StringReplaceNode（截取/替换）
- 组件动作：ScaleObjectNode / SetPositionNode / SetRotationNode（变换）、PlayAudioNode / StopAudioNode（音频）、PlayAnimationNode（动画）——继承 ComponentActionNode<T>
- 新增 Vector3 变量类型：VariableBundle 第 5 容器 + SaveData.vector3s（兼容旧档）+ Get/SetVariableVector3Node + 通讯/持久变量事件补 Vector3 订阅
- LitJSON 对 Vector3 有 Exporter（x/y/z），导入走默认反射（需 Unity 实测确认）

修复：多执行器共享 attachedObject 导致目标错乱

- 新增 NodeExecuteContext（执行上下文）：GraphExecutor 执行每个节点前设入自身、执行后清空（try/finally）
- BaseNodeGraph.GetAttachedObject()：优先取当前执行器对象，其次回退 attachedObject
- ComponentActionNode（Attached/ByName）与 UIComponentResolver（Self）改走 GetAttachedObject()
- 同一张图多个执行器（start/入口分路线）现在各自解析自己的目标物体

Transform 节点重构：泛型组件动作基类

- 新增 ComponentActionNode<T>（BaseNode）：统一目标解析（Attached/ByName/Direct）+ GetComponent + Apply
- MoveObjectNode / RoteObjectNode 改继承 ComponentActionNode<Transform>，类名/菜单不变（图资产兼容）
- 删除 TransformNodeEventExecutor 及 Move/Rote 事件类（不再需要事件中间层）
- 注意：开始2 场景若挂有 TransformNodeEventExecutor 组件，Unity 会显示 Missing Script，需手动移除

新增日志收敛 + 字符串运算节点

- NodeLog 统一日志工具（Error/Warning/Info/Verbose 分级，默认 Warning）+ 菜单 Tools/节点系统/日志级别 切换
- 收敛噪声日志：变量 Get（Verbose）、节点执行（Verbose）、GraphCommunicator 通讯（Info）等
- 新增字符串节点（StringNodes）：StringOpNode（拼接/大小写/去空格）、StringCompareNode（等于/包含/开头/结尾→bool）、StringLengthNode（长度→int）

新增条件等待（两层设计）

- ConditionWaitNode 基类：OnWaitStart（准备）+ CheckCondition（条件）+ timeout（超时，0=不限）
- WaitUntilNode（流程/等待条件）：等待 condition 输入端口为 true，可由比较/逻辑/变量节点组合，零代码
- 系统级条件（对话选项/动画完成等）后续继承 ConditionWaitNode 实现 CheckCondition 即可

新增执行触发策略 + 修复多执行器共享游标

- GraphExecutor 新增 triggerPolicy：Restart（默认，重触发=停止并重跑）/ IgnoreWhileRunning（运行中忽略）/ Queue（运行中排队，跑完自动再跑）
- 执行游标从图资产（BaseNodeGraph.CurrentNode）移到执行器实例私有，多执行器跑同一张图互不干扰
- 移除 BaseNodeGraph.CurrentNode（仅执行器使用，已无用）；协程自然/异常结束均清空引用

新增变量持久化开关（persist）

- Variable<T> 新增 persist 字段（默认开启）：关闭后不进存档、读档不覆盖，每次开始游戏重置为列表初始值
- VariableContainer.ImportFrom 改为合并式：先以列表默认值建字典，再覆盖持久化变量（非持久化保持默认值；存档缺失的列表新变量也以默认值出现）
- Export 跳过非持久化变量；开关对图/房间/全局变量通用

新增等待节点（WaitNode）+ 执行器协程钩子

- BaseNode 新增 GetFlow() 协程钩子（默认 null，零影响现有节点）
- GraphExecutor 链式执行在每个节点后 yield 节点协程（等待等可暂停链，不阻塞进程）
- 新增 WaitNode（流程/等待）：等待指定秒数后继续，为以后"等待条件/对话选项"预留同一机制

新增数学运算节点（四则运算 + 比较）

- MathOpIntNode / MathOpFloatNode：加/减/乘/除（含除零保护），菜单 数学运算/整数运算、浮点运算
- CompareIntNode / CompareFloatNode：等于/不等/大于/大于等于/小于/小于等于（输出 bool），菜单 数学运算/整数比较、浮点比较
- 新增 MathNodes 文件夹；比较节点放 LogicNodes

新增入口节点（EntryNode）+ 图执行模式

- 新增 EntryNode（基本/入口）：带标识符 + 自动 GUID，一张图可放多个
- BaseNodeGraph.GetEntryNode(id) / GetAllEntryNodes()：运行时/编辑器实时扫描 nodes
- GraphExecutor 新增执行模式：Default（startNode）/ Entry（按标识符或 GUID 从入口开始，未找到报错不执行）
- GraphExecutor 自定义 Inspector：入口下拉选择器 + 标识符改名自动回填（编辑模式）
- Get/Set/入口节点的 GUID 只在检查器显示，节点图视图节点体上隐藏（避免误触）

近期修复（存档 / 编辑器 / 节点）

- 存档：Commit 后清空预备层；场景改名兜底命中后删除旧名称存档
- 存档：拆出 SaveSystem.LoadGlobal（只加载全局变量），启动改由它调用，房间存档由进房间时应用，不再重复加载
- RoomIdentity 补齐工具：过滤失效 Build Settings 场景条目 + 单场景容错；场景已挂载 RoomIdentity、禁用失效 SampleScene 并登记 002
- 编辑器模式：UI 通讯节点（Canvas 源）只读扫描场景 UI（不创建、不修改场景），悬停提示不再报错
- StringConditionNode 改为 DataNode（纯数据节点）+ NodeTint 区分
- 开始场景图资产合并：ShowNameText / What 合并为通用节点图.asset

文档整理：重写架构说明与全部 README + Base/xNode 目录清理

- 重写仓库根《架构说明.md》《README.md》与 Event/Music/Save/UI/Variable/xNode 子系统 README
- 删除 xNode/Nodes 下重构遗留的 5 个空文件夹及其 meta
- PersistentVariableEvent.cs 移入 CommunicationNodes/（与 ComSetAndGetVariableEvent 并列）

变量/场景 GUID 追踪（抗改名）+ RoomIdentity

- VariableContainer 新增 nameToGuid/guidToName 映射 + TryResolve/TryResolveAndSet（名字优先 + GUID 兜底）
- 存档格式 v2：变量条目改为 name+guid+value（VariableEntryData），旧存档不兼容
- Get/Set 节点隐藏 variableGuid 字段，运行时自动记录/修正（名字↔GUID 双向适配）
- 新增 RoomIdentity 组件（roomId + 直接引用房间变量资产），场景改名不影响加载
- SaveSystem 房间存档记录 roomId，场景改名后按房间ID扫描 archive 兜底匹配

UI通讯节点统一（UISource/UIType枚举）+ 新增UICollector

- 新增 UICollector（Canvas 单例）：收集 Text/TextMeshPro/Image，按物体名索引，同名警告
- UI 节点统一为 ComUIGetTextNode / ComUISetTextNode（source=自身/Canvas，type=Text/TextMeshPro）
- Get 节点新增 stripRichText 富文本剔除选项
- 删除旧 UICommunicator + 旧事件 + 旧 4 个自身UI节点

统一get/set变量节点为单一基类（source枚举）

- 新增 VariableSource 枚举（本图/跨图/房间/全局）
- GetVariableNode<T> / SetVariableNode<T> 统一基类，按 source 分发
- 具体节点从 24 个收敛到 8 个，加新类型只需 2 个文件
- 删除旧的本图/跨图/作用域 get/set 节点（30 个文件）

新增物品持久化系统（PersistentItem + PersistentItemManager）

- 新增 PersistentItem 基类：直接持有 VariableBundle，itemId 自动生成 GUID
- 移除 OnSaveState/OnLoadState，保留 OnBeforeSave/OnAfterLoad 可选钩子
- PersistentItemManager 单例：收集/分发每房间物品状态
- SaveSystem 房间存档接入物品数据（CaptureRoom/ApplyRoomSaveNow）